# GOAL：PaperPilot 产品体验优化（执行版）

## 目标
把 PaperPilot 从「功能可用」打磨成「成熟产品」：截止 2026-09-05，新用户 10 分钟内独立完成首次写作闭环（定题→采用大纲→写第一段→AI 润色→插入引用→打卡），全程无卡点、无指点；核心旅程任意状态下都知道「下一步做什么」；每个操作有反馈；全模块零崩溃零报错；桌面与移动端（390px）均可用。

## 现状与痛点
功能已全（四大模块+论文主页+设置，v2.10），视觉已重设计（v3.0 纸墨朱砂）。但真机使用「不顺手、不成熟」：功能在却不知该做什么、做完去哪；部分交互仍是「表单+结果框」工具感；状态反馈、空态、错误、中断恢复打磨不足。

## 优化范围
六大视图（论文主页 / 选题与大纲 / 写作工作台 / 文献中心 / 计划与进度 / 设置）+ 五条核心任务链：
1. 首次配置（填 Key、测试连接）
2. 选题采用（生成建议→设为题目→生成大纲→采用）
3. 按章写作（选章→写→AI 动作→标记完成）
4. 文献入库并引用（批量推荐→勾选入库→工作台插入 [n]）
5. 计划与打卡（设截止→生成甘特图→每日打卡）

## 验收标准（全部量化）
- KR1 首次闭环 ≤ 10 分钟（新用户、无文档、无指点）
- KR2 五条任务链走查一次性完成率 100%
- KR3 缺陷清单 P0（卡死旅程）/P1（明显别扭）清零；六类：衔接可见性 / 状态反馈 / 空态引导 / 错误提示 / 防误操作 / 中断恢复
- KR4 自动化回归零控制台错误、零渲染失败、零横向溢出（桌面+移动端）
- KR5 390px 移动端核心旅程（写作、打卡）可完成
- KR6 反馈闭环：点必有应、等必有进度、成必有标记（按钮态 / 结果框 / toast / 印章）

## 非目标与边界
- 不引入后端 / 账号 / 云同步（保持纯静态 BYOK 架构）
- 不做整篇代写与 AIGC 检测（合规红线）
- 视觉基调不变：「纸墨朱砂」体系保持统一；但单个功能板块的布局与交互模型支持重构（有更好设计时，先给方案再动手）
- 新功能模块：允许新增，前提是符合产品定位（全流程 / 计划管理 / 国标引用合规）且用户价值明确；每个新功能先提一句话方案（解决什么问题 + 入口在哪 + 工作量估计），作者拍板后实施，避免范围蔓延

## 设计准则（既往 7 轮反馈提炼，改任何交互先对照这 7 条）
1. 衔接可见可点：数据层衔接必须有界面层入口与去向
2. 编辑器优先：AI 动作作用于选中文字，消灭「表单+结果框」工具感
3. 一次到位：批量+审核优于逐项操作
4. 可解释：AI 输出必须带理由/出处，用户能判断
5. 按使用时机切分界面：在「用」的地方给工具，不在「管」的地方堆功能
6. 点必有应：点击有反馈、等待有进度、完成有标记
7. 空态即邀请：每个空状态都给下一步行动按钮

## 工作方式
- 自主推进、不逐项问询；每轮闭环：走查出缺陷清单（P0/P1/P2 分级）→ 修复 → Playwright 自动化回归
- 每轮修复后更新 report/dev-log.md（人提的问题 → AI 的改动 → 留待作者评价）
- 暂停条件：出现新功能提案、板块重构方案等需作者决策的事项；或产出可验收版本时提醒作者
- 节奏：8/17-20 体验审计 → 8/20-29 修复迭代 → 8/30-9/5 验收收尾

## 执行进度
- 2026-08-17 第一轮 ✅：审计 16 项缺陷（P0×2/P1×7/P2×7，含 3 个历史 bug）→ 全部修复；板块增强 8 项；新功能「数据备份导出/导入」经作者拍板实施；回归 51 项断言全过（详见 report/dev-log.md 第 19 次会话）
- 2026-08-18 第二轮 ✅：走查 9 项全实施——选区快捷操作条（Notion AI 式落实）、页眉就地标记本章完成、Ctrl/Cmd+S 立即保存、初稿面板自动聚焦、入库后结果即时刷新、录入保存后清空、大纲解析失败报错、主页热力图共用、标签页动态标题；回归 56 项全过（第 20 次会话）
- 2026-08-18 第三轮 ✅（作者报告撤销 bug）：撤销修复——程序化编辑改 execCommand('insertText') 用户编辑路径，AI 动作/初稿/引用插入全部可原生 Ctrl+Z；「填入示例试试」×2；设置载入演示自动回主页；初稿确认显示字数；旅程完成徽章；回归 61 项全过（第 21 次会话）
- 2026-08-18 第四轮 ✅（作者要求"继续打磨整体用户体验"）：中断恢复——导航时取消进行中的 AI 请求（此前静默丢结果+谎报"已应用"）、响应与切页竞态时明确提示；修复事件总线 bug（tm:navigate 分发在 document 不冒泡 + 侧栏导航绕过事件，已统一）；摘要按钮状态机（生成中禁用/未生成置灰）；打卡按钮 disabled 说明；移动端工作台横向溢出修复（grid 单列 minmax(0,1fr)，此前 1fr 隐式 min-content 被长文本/长选项撑爆）；移动端写作旅程深测（KR5 验证：打字→选区→切章→打卡全通过）；回归 108 项断言四套件全过（第 22 次会话）
- 2026-08-18 第五轮 ✅（作者要求"继续打磨优化整体的用户体验"，本轮跳过真机验收）：选题页按钮状态机（未生成时复制/采用置灰，生成后启用）；**采用流程断链修复**——采用后重新生成大纲时印章不再挡路，自动恢复「采用此大纲」入口（此前无法再次采用新大纲）；文献检索中断恢复——批量推荐/单次检索随导航取消（fetch 与 AI 调用全透传 signal，aborted 静默），不再浪费 token 与写进已卸载页面；文献勾选热区扩大为整条（label 包裹，点击标题行切换勾选）；回归 134 项断言五套件 + 移动端 390px 无溢出全过（第 23 次会话）
- 2026-08-18 第六轮 ✅：文献中心与设置页——AI 解析中断恢复（**aborted=不入库不打扰**，与写作/文献模块共用 `__tmAbort` 幂等共享 controller；此前切页后解析照跑+假提示已保存）；连接测试中断恢复（testConnection 支持外部 signal）；**手动录入实时预览**（desc 承诺「实时生成」实际保存才有——六个字段防抖 300ms 实时出 GB/T 7714，题名清空回占位）；防抖竞态深层 bug（保存清空字段后待触发 timer 以空表单覆盖预览 → clearTimeout 修复）；回归 155 项断言七套件全过（第 24 次会话）
- 2026-08-18 第七轮 ✅：主页卡片图标统一——线性图标提取共享模块 js/icons.js（v3.0 后主页卡片仍是 emoji 的遗留不一致，侧栏与卡片同源渲染）；距截止 ≤14 天红色数字加 title 提示；写作之旅步骤悬停状态提示；回归 155 项断言七套件全过（第 25 次会话）
- 2026-08-18 第八轮 ✅：计划与进度模块走查（五条任务链最后一条）——甘特图已完成阶段松烟绿+✓ 标记（此前完成无反馈）；撰写阶段章节 chips 显示每章进度状态（计划页同时看到计划与进度）；计划模板补齐 4 种学位（博士 36 周/课程论文 8 周，与选题页学位类型对齐）；短阶段宽度 clamp 溢出保护；打卡预选当前章；过截止文案与主页一致；回归 147 项断言八套件全过（第 26 次会话）
- 2026-08-19 第九轮 ✅：选题与大纲模块走查——**AI 调用中断恢复补全（P1，五条任务链最后一块拼图）**：此前选题/大纲生成不带 signal，切页后请求照跑、结果写进已卸载页面；修复 `__tmTopicAbort` 与五模块同构，至此全部 AI 调用点支持导航取消静默；学位类型下拉回显已存值（此前回退默认本科论文，生成错套模板）；生成中结果框 filled 高亮残留清理；回归 163 项断言九套件全过（第 27 次会话）
- 2026-08-19 第十轮 ✅：写作工作台全文走查——**整章替换数据破坏缺陷（P1，本轮最高严重级，自动化复现）**：未选中状态下润色/扩写/降重与生成初稿的替换区间含章节标题行且 AI 输出无尾换行，导致标题行被删、内容与下一章标题粘连——parseDocument 丢章、防抖保存覆盖 outline（实测 3 章大纲变 1 章）；修复 chapterBodyRange 保留标题行 + 章尾换行补丁，复现验证 3 章全保留；空回复防御、素材复制换行丢失（dataset 属性规范化）、素材 10 条截断提示；回归 177 项断言十套件全过（第 28 次会话）
- 2026-08-19 第十一轮 ✅：设置页与数据模型走查——**连续打卡时区错位**（calcStreak 用 UTC 天序号转本地日期，本地午夜后起点差一天、连续天数算错 → 全改本地日期流转）；设置页 5 处注入面修复（Key/模型名/Base URL/题目回显与测试连接模型回复全部 escapeHtml，模型输出可注入任意 HTML）；清除数据改前缀遍历（硬编码 key 清单会漏新数据）；导出→清空→导入恢复链路完整验证；回归 192 项断言十一套件全过（第 29 次会话）
- 2026-08-19 第十二轮 ✅：文献中心库管理走查——**补号冲突缺陷**（ensureNumbers 用数组下标补号与已有编号撞号，出现两条 [1]，正文引用解析错、复制全部输出重复编号 → 按 max(litNo)+1 递增分配，编号是正文引用 [n] 的稳定键，只增不重排）；**AI 解析三处防御**（非对象元素过滤入库、全非法/空数组显式报错不再假装「已解析 0 条」、解析中 filled 残留清理）；删除引用过文献弹警告已回归验证；回归 211 项断言十二套件全过（第 30 次会话）
- 2026-08-19 第十三轮 ✅：共享基础设施走查（api/storage/ui/gbt7714——被全部模块依赖的四件套）——**res.json 无兜底**（200 但 body 非 JSON 时抛原始 SyntaxError，错误框显示不可读英文 → bad_response 可读文案）；**setLoading 连续 loading 覆盖原文**（orig 存进「生成中…」，按钮永久卡加载态 → 仅首次保存、恢复后释放）；403 未映射（Key 无权限提示）；**storage.set 容量触顶无兜底**（QuotaExceededError 在防抖保存路径抛未捕获错误且用户不知情 → 明确 toast 引导导出备份）；toast 挂载防御；GB/T 7714 六种格式规则单元断言固化；回归 226 项断言十三套件全过（第 31 次会话）
- 2026-08-19 第十四轮 ✅：在线文献查找走查（litsearch 全文 + demo-data 审查）——**批量推荐标注阶段 abort 未接住（P1，unhandled rejection）**：runBatch 的 `annotateCandidates(...).catch(rethrow abort)` 在 try/catch 之外，切页/超时产生 unhandled rejection 且按钮卡「标注中」不恢复 → 就地接住（对比 run() 同款调用在 try 内能接住，runBatch 是唯一漏点）；**数据库直连三处 res.json 无兜底**（CrossRef/OpenAlex 200+非 JSON 时显示英文 SyntaxError → 可读文案）；demo-data.js 审查无缺陷（引用编号与草稿一致）；回归 237 项断言十四套件全过（第 32 次会话）
- 2026-08-19 第十五轮 ✅：复制全文/排版预览交付链走查——**缺号引用静默丢弃（P2，学术规范硬伤）**：正文引用了文献库不存在的编号（如 [3]）时参考文献列表静默缺条，用户无感知直到交论文 → buildRefList 返回 {refs, missing}，复制/预览点击时 toast 指明缺号；预览页内朱砂色提示条（@media print 隐藏，屏幕自查不污染 PDF 成品）；引用完整时零打扰回归验证；回归 249 项断言十五套件全过（第 33 次会话）
- 2026-08-19 第十六轮 ✅：KR1 首次闭环体验审计（GOAL 第一条验收标准）——**设置页「回论文主页看下一步」引导（P1 衔接可见性）**：新用户配完 Key 停在设置页不知道下一步去哪，保存后出现「回主页看下一步 →」按钮（有题目后自动隐藏）；**手动编辑吞并标题行导致部分丢章（P2 防误操作，R10 机器路径教训的手动路径变体）**：光标点在章节标题行首打字时标题被吞进上一行正文，persistDocument 旧判定只保护「全部丢失」、部分丢失时静默用残缺大纲覆盖（实测 3 章变 2 章）→ 升级 lost 数组三元判定（一致→覆盖 / 部分丢失→保留原大纲+朱砂警告 / 清空→覆盖），新增章节改名拆分不受误伤；新增 Round 16 套件 33 项断言（空数据五步旅程 0/5→5/5、hero 行动链状态机、设置页引导显隐、标题行首插入结构保护、全程零 console 错误）；全量回归 282 项断言十六套件全过（第 34 次会话）
- 2026-08-19 第十七轮 ✅：移动端 390px 全量回归（KR4/KR5 验收项）——上次移动端深测是第四轮，此后 13 轮改动（旅程条/选区工具条/回主页引导/缺号提示条等新元素）未再 390px 验证；本轮六视图逐模块零横向溢出（含长文案 toast 边界）、移动端写作旅程真实完成（打字→选区工具条在视口内→切章→打卡→插入引用）、零 console 错误；**0 产品缺陷，KR4/KR5 移动端验收项成立**；修正 3 项测试方法论（drafts 值为 {content,updatedAt} 对象结构、execCommand insertText 依赖 textarea 焦点、4 个选择器类名）；新增 Round 17 套件 37 项断言；全量回归 319 项断言十七套件全过（第 35 次会话）
- 2026-08-19 第十八轮 ✅：跨模块数据一致性审计（KR2）——设置页改题目 → 主页徽标实时同步 + 写作文档首行/hero 重渲染同步；改截止日期 → 主页距截止天数 + 计划页刷新；改学位类型 → 选题页回显（大纲模板口径）；清空数据 → 六模块空态全恢复（徽标未设置/旅程 0/5/写作无大纲引导/文献暂无/计划空态/数据清零）；演示数据重载 → 旅程 5/5 满态恢复；**0 产品缺陷**（数据同步模式「事件即时刷新 + 模块重渲染读取」一致成立，无需中央 store）；新增 Round 18 套件 18 项断言；全量回归 337 项断言十八套件全过（第 36 次会话）
- 2026-08-19 第十九轮 ✅：性能审计——量化基线（首屏 79ms、模块切换 3-19ms、**96K 字文档**防抖保存 <2.5s + 打字 20ms + 复制全文 312ms、365 天热力图 217ms、甘特图 575ms、去重 Set O(n)），**0 性能缺陷**（纯静态架构无瓶颈）；性能验收口径三档「交互即时 <200ms + 重型操作 <2s + 数据上限 10 万字」写入断言套件；新增 Round 19 套件 10 项断言（宽松阈值防 CI 抖动）；全量回归 347 项断言十九套件全过（第 37 次会话）
- 2026-08-19 第二十轮 ✅：AI 提示词质量审计——**四动作缺论文上下文（P2）**：润色/扩写/降重/续写此前只收到所选文字、没有题目与章节，AI 不知在写什么论文；新增 ctxLine helper 把「论文《题目》「章节」」注入四个 prompt（无题目退化「（未定题）」），runAction 注入 {title, chapter}；其余五调用点（初稿/摘要/选题/大纲/解析）上下文确认完整；观察记录「AI 动作作用于题目行会同步改写项目题目」（行为一致可解释，暂不修）；新增 Round 20 套件 15 项断言；全量回归 362 项断言二十套件全过（第 38 次会话）
- 2026-08-19 第二十一轮 ✅：**KR1 首次闭环端到端计时走查（GOAL 第一条验收标准的直接验收）**：从「已配 Key、无题目、无文档」空数据起点 mock AI 走完新用户全链路——定题（方向→生成→设为题目→徽标同步）→ 大纲（生成→采用→印章）→ 写第一段（用户路径插入→落盘）→ 润色（写入+toast）→ 插入引用（解析入库→下拉→[1]）→ 计划打卡（甘特图→落库）→ 旅程 5/5 满态；**自动化口径 8.6s、真实 AI 预计 2-3 分钟，KR1 成立**；0 产品缺陷（两处失败均为断言过严：徽标 12 字截断、印章两处）；新增 Round 21 套件 15 项断言；全量回归 377 项断言二十一套件全过（第 39 次会话）
- 2026-08-19 第二十二轮 ✅：AI 输出容错走查（真实模型输出的 markdown 包裹/尾随逗号/前缀杂讯是高频形态）——**parseOutline 容错（P2 章节丢失）**：`**第一章**`/`## 标题`/`- 列表` 此前锚定「第」字全不识别 → 清洗 markdown 修饰 + 编号变体 `1、`/`1．`（清洗保留数字前缀交 num 分支，第一版误剥已修正）；**parseSuggestions 容错（P2 题目星号污染）**：`1. **标题**` → title 带星号进徽标 → 清洗 `**`/`__`/反引号；**citation 解析对齐 litsearch 模式（P2 误拒收）**：前缀说明/```json 围栏/尾随逗号/包装对象 → 两段式容错（先直接解析→提取数组+去尾随逗号→解包装）；新增 Round 22 套件 13 项脏输出断言；全量回归 390 项断言二十二套件全过（第 40 次会话）
- 2026-08-19 第二十三轮 ✅：可访问性审计（成熟产品底线）——**toast 无读屏语义（P2，KR6 对读屏失效）**：全站反馈中枢无 aria-live → `#toast-wrap` 挂 `role="status"`+`aria-live="polite"`（容器已在初始 DOM，一处覆盖全站反馈）；键盘可达性 0 缺陷（六视图 focus 可达、12 次 Tab 焦点链不断、Enter 激活导航）；纸墨朱砂七组配色 WCAG 对比度全部 ≥4.5:1（最小 4.60），算法固化进断言防换色回归；新增 Round 23 套件 13 项断言；全量回归 403 项断言二十三套件全过（第 41 次会话）
- 2026-08-19 第二十四轮 ✅：**KR2 五条任务链一次性走查（第二条验收标准的直接验收）**：从「连 Key 都没有」空起点一次走完五链——首次配置（Key→测试→保存→回主页引导）、选题采用（生成≥2 条→设题→大纲→采用→去写作衔接）、按章写作（写→润色→标记完成）、文献批量推荐（批量 4 条→全选→入库→插入 [1]，**首次完整链套件**）、计划打卡（甘特图→打卡→今日已打卡）；链尾旅程 5/5 满态；**0 产品缺陷，KR2 成立**（两处断言修正：选题需 ≥2 条才渲染按钮、完成按钮是隐藏非移除）；新增 Round 24 套件 19 项断言；全量回归 422 项断言二十四套件全过（第 42 次会话）
- 2026-08-19 第二十五轮 ✅：验收交付物落地——docs/acceptance.md 三层结构（六条 KR 自动化验收状态表 + 真机勾选清单 + 两项已知观察项），作者打开即知哪些已由 422 项断言兜底、哪些需真机自测；自动化覆盖 422 项断言 24 套件全过（第 43 次会话）
- 2026-08-19 第二十六轮 ✅：边界输入审计（成熟产品「不崩」底线）——超长题目 216 字（徽标截断不崩/完整入库/首行渲染）、空值与纯空格防御、超长文献标题 360 字入库无溢出、边界日期（已过截止提示/远古日期不崩）、重复打卡禁用、HTML 注入转义不执行（R11 回归确认）；**0 产品缺陷**；新增 Round 26 套件 14 项断言；全量回归 436 项断言二十五套件全过（第 44 次会话）
- 2026-08-19 第二十七轮 ✅：错误文案统一审校——**批量检索失败归因误导（P2）**：AI 策略输出格式异常与网络/Key 错误共用「可到设置检查 API Key」toast，AI 输出问题时误导用户查 Key → 透传 R13 可读错误文案、格式异常分支明确说明已自动退回原始关键词；「请先…」句式全站九处统一确认；无上下文时批量按钮条件隐藏；新增 Round 27 套件 12 项断言；全量回归 **448 项断言二十六套件全过**（第 45 次会话）
- 2026-08-19 第二十八轮 ✅：文献库引用状态自查（数据量增大后的可用性缺口）——每条文献标注「已引用/未引用」chip + 统计行「已引用 X / N 条 · 未引用 K 条（GB/T 7714 参考文献应为正文实际引用项，建议补引或删除）」；全部被引用时转松烟绿「全部已被正文引用 ✓」；手动录入/删除即时刷新、草稿引用变化重挂载更新；空库不渲染统计行；新增 Round 28 套件 14 项断言；全量回归 462 项断言二十七套件全过（第 46 次会话）
- 2026-08-19 第二十九轮 ✅：学位目标字数提示（写作硬性要求从未被产品提示）——TARGET_RANGE 学位常见区间（本科 1.5-3 万/硕士 3-5 万/博士 8-15 万/课程论文 5 千-1 万）+ 工作台状态行「距下限还差 X 字」（朱砂）→ 达下限松烟绿「已达下限 ✓」；各章草稿合计口径与目录 chip 一致；输入防抖落盘后刷新、初始渲染即填充；未知学位/无大纲不显示；新增 Round 29 套件 9 项断言；全量回归 471 项断言二十八套件全过（第 47 次会话）
- 2026-08-19 第三十轮 ✅：撤销/重做完整走查（用户早期反馈「撤销不生效」的系统回归）——AI 润色可撤销（applyEdit 进原生撤销栈）、重做恢复、撤销粒度正确（只回退新输入）、多步回退、撤销链后防抖落盘与编辑器一致；**0 产品缺陷**；测试方法论修正：headless 键盘模拟不触发 textarea 原生撤销（用 focus+execCommand('undo') 等价）、线性撤销栈语义（撤销后撤销=重做之前操作，Word 标准行为）；新增 Round 30 套件 10 项断言；全量 481 项断言二十八套件（R24/R17/R20/R21/R29 回归确认）（第 48 次会话）
- 2026-08-19 第三十一轮 ✅：插入引用体验增强（文献多时原生下拉无法查找目标文献）——**未引用文献排序优先**（大概率引用尚未引用的文献，减少重复）+ option 补「· 未引用」标记；**编号直达输入**（`#w-cit-no` 数字框，知道编号直接输、Enter 即插，优先于下拉当前值，插入后清空）；无效编号拦截（朱砂 toast「文献库中没有编号 [99]」不插入）；新增 Round 31 套件 13 项断言；全量回归 **494 项断言三十套件全过**（第 49 次会话）
- 2026-08-19 第三十二轮 ✅：全文下载为 Markdown（论文最终要交 Word/PDF——「复制全文」只进剪贴板没有落文件出口）——工具栏「下载全文」：复用 fullText（题目+摘要+关键词+各章+自动参考文献+致谢）Blob 下载；文件名非法字符（\ / : * ? " < > |）替换下划线、空题目兜底「论文全文.md」；缺号提示与复制/预览同款；下载前先落盘；新增 Round 32 套件 13 项断言；全量回归 507 项断言三十一套件全过（第 50 次会话）
- 2026-08-19 第三十三轮 ✅：文献库实时搜索（50+ 条后库内找文献靠滚动，与 R31 同源缺口的另一半）——`#cit-search` 按 标题/作者/出处/完整格式 任一包含匹配（大小写不敏感）；输入即过滤、搜索词在局部刷新中保留；匹配计数「匹配 X / N 条」零匹配转朱砂；✕ 清空回焦点；空库禁用；新增 Round 33 套件 17 项断言（第 51 次会话）
- 2026-08-19 第三十四轮 ✅：触屏撤销/重做按钮（iPad/触屏笔记本无快捷键，R30 验证的 Ctrl+Z/Y 对触屏用户不可达）——工具栏「↶ 撤销」「↷ 重做」点击 = focus + execCommand('undo'/'redo') 同原生撤销栈；成功 toast；390px 无溢出；新增 Round 34 套件 10 项断言（第 52 次会话）
- 2026-08-19 第三十五轮 ✅：文献检索「加载更多」分页（单次检索默认 10 条候选不够选）——searchLiterature 系加 offset 参数；列表底部「加载更多」追加下一页去重、总数更新、无更多自动收尾；追加渲染保留已勾选（insertAdjacentHTML 不重绘）+ 首屏「推荐前 3」只生效一次；勾选计数/详情改为事件委托；lit-add 引用 currentItems（追加条目可入库）；批量推荐不分页；**实现期修复 1 个真 bug：追加批次下标从 0 重算导致 id 冲突**；新增 Round 35 套件 14 项断言；全量回归 **548 项断言三十四套件全过**（第 53 次会话）
- 2026-08-20 第三十六轮 ✅：素材一键插入正文（素材库此前只有复制/删除——保存素材的意义是复用，缺了最直接的「插进正文」交互）——素材条目「插入」按钮：编辑器光标处 applyEdit（与引用插入同款，进原生撤销栈可撤销）、多行格式原样保留、插入后落盘一致、空素材库不渲染按钮；新增 Round 36 套件 10 项断言；全量回归 **558 项断言三十五套件全过**（第 54 次会话）
- 待办候选：**真机验收（作者自测）**——清单已落地为 docs/acceptance.md（六条 KR 自动化验收状态表 + 五任务链/数据安全/移动端真机勾选清单 + 两项已知观察项）；自动化覆盖 558 项断言 35 套件全过，真机部分需作者用真实 DeepSeek Key 走查；**移动端真机重点**：软键盘是否遮挡选区工具条、低端手机首屏/切换时间；后续轮次继续走查（见各轮 dev-log）
