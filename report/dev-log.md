# 开发日志（AI 使用报告素材）

> 用途：如实记录每个开发日「人做了什么、AI 做了什么、效果如何」，作为《AI 使用报告》的一手数据来源。
> 每次开发会话结束时追加一条，由 AI 填写事实、由作者补充评价（人工修改率、质量评估等）。

## 2026-08-15 · 阶段① 立项与设计（第 1 次会话）

- **人的工作**：
  - 确认作业要求：2~3 周工期、真实 API 接入、四大功能模块全覆盖
  - 关键决策：纯静态无后端（GitHub Pages / Netlify 托管）、使用 DeepSeek API Key、MoSCoW 范围控制
- **AI 的工作**：
  - 输出项目总体方案（WBS + 三周里程碑 + 风险登记册）
  - 起草 PRD v1（docs/PRD.md）
  - 搭建项目骨架与可点击的页面原型（index.html + 6 个模块页面）
- **人工修改**：（待作者补充：布局、命名、功能取舍等调整意见）
- **质量评估**：（待作者补充：AI 产出可用度、修改比例）
- **用时**：（待作者补充）

## 2026-08-15 · 阶段0-1 市场/竞品/用户调研（第 2 次会话）

- **人的工作**：
  - 纠偏开发节奏：要求按成熟商业产品流程推进（市场→竞品→用户→产品定义→原型→开发，阶段关口制）
  - 决策：用户研究采用桌面研究（不打扰同学）；选择"编辑自动放行+只读白名单"的权限配置
- **AI 的工作**：
  - 派出 3 个并行调研任务：国内竞品（15个）、国外竞品（9个）、学生痛点桌面调研，全部落盘 docs/research/
  - 修正项目计划为 6 阶段关口制；配置项目级权限白名单
- **调研核心结论**：写作计划管理是行业空白；GB/T 7714 格式存在断层；学生核心焦虑为查重+AIGC率；AI生成付费意愿低、验证了 BYOK 免费模式
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-15 · G0 关口评审（第 3 次会话）

- **人的工作**：审阅三份调研报告（HTML 渲染版）；拍板 G0 两项决策（定位叙事、合规立场）；提出"md 可读性差"问题驱动报告渲染工具
- **AI 的工作**：整合《市场与竞品分析报告》（TAM/SAM/SOM 商业测算、商业模式对比、SWOT）与《用户研究报告》（3 画像、MoSCoW 分级）；开发零依赖 MD→HTML 报告渲染工具（tools/render-reports.js）
- **决策记录**：① 定位 = 覆盖论文全流程、唯一内置写作计划管理、国标引用合规的轻量助手；② 合规立场 = 诚信提示、不做整篇代写、不做 AIGC 检测集成
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-15 · G1 评审通过 + 阶段2 产品定义（第 4 次会话）

- **人的工作**：审阅 MoSCoW 分级与 Won't 清单，全部批准；指示继续推进
- **AI 的工作**：PRD 重写为 v2（调研数据替换假设、新增合规立场与实现方案、里程碑更新为六阶段关口制）；原型按新定位同步改造（全局学术诚信提示条、页脚诚信声明、定位文案、降重改写合规表述）；报告渲染工具纳入 PRD
- **决策记录**：G1 通过——Must 13 / Should 3 / Could 2 / Won't 3（整篇代写、AIGC 检测、账号云同步）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-15 · G3 评审 + Sprint 1 启动（第 5 次会话）

- **人的工作**：原型评审反馈——功能可用但模块割裂、衔接不起来，与"全流程"定位有落差；确认采用"论文项目主线 + 四链路"方案；纠正本地运行方式（file:// 下 ES 模块被浏览器拦截，需静态服务器）
- **AI 的工作**：PRD 增补 §4.4 模块衔接设计并升级 v2.1；新增 tools/serve.sh 本地预览脚本并启动 localhost:8000；Sprint 1 启动——实现论文项目状态层（js/project.js）与「论文主页」仪表盘重构
- **决策记录**：G3 通过 + 范围变更——衔接层按推荐方案（四链路，不新增阶段；进度挤压时先砍 Could 项）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-15 · Sprint 1+2 开发完成（第 6 次会话，自主开发模式）

- **人的工作**：指示"不遇关口不停"的自主开发模式（此为效率关键决策，将写入 AI 使用报告）
- **AI 的工作**：
  - Sprint 1：论文项目状态层（js/project.js，含大纲解析器/连续打卡统计）、论文主页重构、文献库完整 CRUD、GB/T 7714 规则引擎（纯规则、node 冒烟测试通过）、甘特图按截止日期真实倒排（大纲章节自动挂载撰写阶段）、打卡关联章节进度
  - Sprint 2：选题建议/大纲生成/摘要/润色/扩写/降重改写全部接入 DeepSeek API（含合规约束提示词）；文献 AI 解析（AI 结构化 → 规则引擎格式化 → 批量入库）；写作辅助"插入引用"（链路②）；设置新增论文基本信息（链路④）；顶部论文徽标
  - 风险验证：curl 实测 DeepSeek CORS 预检通过（access-control-allow-origin 正常返回），风险登记册头号风险关闭
  - 质量验证：11 个 JS 文件 node --check 全部通过；本地服务器全资源 200；格式化器与解析器逻辑冒烟测试通过
- **决策记录**：无（范围内执行）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-16 · G4 第一轮反馈 + v2.2 重设计（第 7 次会话）

- **人的工作**：G4 第一轮验收反馈——功能设计简陋、模块衔接不可见；指示重新设计打磨、部署推后
- **AI 的工作**：
  - 诊断：v2.1 的衔接只做在数据层，界面仍是"各自为政的表单"，衔接对用户不可见 ≈ 没有衔接
  - v2.2 重设计：写作工作台（三栏整合视图：大纲章节导航+状态点+标记完成 / AI 工具带"正在写第X章"上下文 / 文献库插入引用+素材库）；选题建议解析为卡片可「设为论文题目」；论文主页每章「去写」直达+「继续写作」主按钮（记住上次进度）；素材库增删查
  - PRD 升级 v2.2（变更记录第 7 条、链路⑤⑥、工作台设计）；全部 11 个 JS 文件语法检查与模块加载测试通过
- **决策记录**：G4 未通过 → 进入 v2.2 打磨迭代；部署按用户要求推后
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-16 · G4 第二轮反馈 + v2.3 交互模型重构（第 8 次会话）

- **人的工作**：指出工作台交互模型错误——"到这里应该正式写作了，不该生成摘要"；给出明确设计方向：像其他应用一样有一个整体的文章文本框，用户在其中连续撰写初稿，润色/扩写/降重是对文章的操作，而非独立表单
- **AI 的工作**：v2.3 重构——工作台中栏改为大文本框编辑器（自动保存、实时字数、首次动笔自动标记"进行中"、各章字数显示）；AI 改为作用于选中文字的四个动作（润色/扩写/降重/续写，结果替换或插入文档，可 Ctrl+Z 撤销）；摘要生成移到右栏成为"基于草稿"的后期工具；文献插入改为插入到编辑器光标处；PRD 升级 v2.3（变更记录第 8 条）
- **决策记录**：G4 第二轮未通过 → 交互模型从"表单+结果框"改为"编辑器+AI动作"（Notion AI 式）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-16 · v2.4 新增章节初稿生成（第 9 次会话）

- **人的工作**：提出关键产品质疑——"不支持生成初稿，客户还是要从头自己写文章？"（暴露了写作辅助的完整性缺口）
- **AI 的工作**：v2.4 增补——工作台新增「✨ 生成初稿」：基于论文题目+当前章节大纲+用户要点+目标字数生成本章草稿，写入编辑器（已有内容时确认覆盖），人工逐段审阅修改；系统提示词合规边界明确化（允许章节草稿辅助、禁止整篇代写）；PRD 升级 v2.4（Must 增至 15 项、行为分层灰色区补充、变更记录第 9 条）
- **决策记录**：合规边界——"基于用户大纲/要点的章节草稿"=辅助（允许），"一键整篇代写"=红线（禁止）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-16 · v2.5 在线文献查找（第 10 次会话）

- **人的工作**：提出产品质疑——"文献库只能手动添加？生成稿件时不能自动找文献验证后入库？或选题后自动查找、客户审核选择？"（指出被动文献管理缺口，并给出"自动找→人审核→入库"的期望流程）
- **AI 的工作**：v2.5 实现——接入 CrossRef 真实学术数据库（免费无 Key、浏览器直连、实测可用）；三个入口：选题后推荐（默认检索词=论文题目）、写作工作台右栏（默认=题目+当前章节）、文献模块独立检索；交互=按相关性列出（默认勾选前3条）→ 显示期刊/年份/DOI 供核对 → 勾选后经 GB/T 7714 规则引擎格式化入库；GB/T 7714 引擎抽为独立模块 gbt7714.js 供复用；PRD 升级 v2.5（链路⑦、Must 增至 16、风险表新增 CrossRef 依赖行、变更记录第 10 条）
- **决策记录**：分工原则——机器负责"找真实的"（真实数据库元数据杜绝 AI 幻觉引用），人负责"审核选择"；对应调研发现"主流 AI 工具引用真实性无保障"的差异化机会
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-16 · 文献查找故障修复（第 11 次会话）

- **人的工作**：测试发现"写作台自动查找文献用不了"——G4 真机验收抓到线上问题
- **AI 的工作**：定位——curl 带 Origin 实测 CrossRef 与 OpenAlex 均返回 access-control-allow-origin（浏览器直连无跨域障碍），问题大概率是①浏览器缓存旧版 JS（原服务器不设缓存头）②单一数据源无容错；修复——litsearch 改为双数据源自动切换（CrossRef 主源/OpenAlex 备源）、错误信息含"强制刷新"引导；服务器换为 tools/serve.py（Cache-Control: no-store，杜绝缓存）；双源冒烟测试通过（5 条真实文献含 DOI）
- **教训**：此前"已实测打通"是在 Node 环境测的（无同源策略），未验证浏览器 CORS——环境差异是 AI 测试的盲区，应始终以目标环境（浏览器）为准验证
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-16 · 工作台"卡死"致命 Bug 修复（第 12 次会话）

- **人的工作**：G4 真机测试发现——写作工作台进去后完全无响应，查找文献区没有查找按钮
- **AI 的工作**：定位——writing.js 的 renderRight 函数引用 p.title 但函数内未定义 p（v2.3 重构时遗漏），ReferenceError 导致 render() 中断：所有按钮监听器未绑定（=整页无响应）、右栏渲染半途而废（=查找按钮缺失）；修复——补上 getProject()；同时加两层防护：右栏渲染 try/catch（失败只影响右栏并显示错误），app.js switchModule 全局 try/catch（任何模块渲染失败显示可见错误卡片而非静默冻结）；扫描确认其他模块无同类问题
- **教训**：语法检查（node --check）覆盖不了"渲染路径运行时错误"——纯前端无构建的项目缺运行时冒烟测试，应建立最小 DOM 测试或至少人工过一遍每个模块渲染
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-16 · v2.6 批量文献推荐与详情交互（第 13 次会话）

- **人的工作**：两轮反馈——①逐章查找不便，应按"文章标题+大纲"统一推荐一批文献；②候选文献只能看大概，应能点进去看原文；③整体 UI 交互需要再调
- **AI 的工作**：v2.6——批量推荐（论文题目+各章并行检索、按章节分组、跨组按 DOI/题名去重、最多 6 组，冒烟测试 3 组并行 9 条含 DOI）；候选条目重构：🔗 原文按钮（DOI 全文页跳转，无 DOI 回退学术搜索）、详情展开（完整 GB/T 7714 格式+DOI+摘要）、已在库中标记（禁用+跳过重复）、数据来源徽标；三入口均支持批量推荐（选题页/文献模块），工作台右栏保留章节级检索并引导去批量入口；PRD 升级 v2.6（链路⑦细化、变更记录第 12 条）
- **决策记录**：批量推荐与章节级检索并存——"整篇统一推荐"与"写到哪查到哪"是互补场景
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-16 · v2.7 AI 检索策略与推荐理由（第 14 次会话）

- **人的工作**：质疑匹配质量——"推荐的文献感觉不搭配"，并提出关键产品要求：**推荐必须指出"哪个点可被用来印证什么"，否则用户无法选择**
- **AI 的工作**：v2.7 三段式推荐流水线——①AI 检索策略（论文题目+大纲→英文学术检索词，解决中文语境匹配英文数据库的难题）②真实检索+合并去重 ③AI 推荐理由标注（每条候选标注"可印证/支撑哪个点"+匹配最适配章节，按章节分组展示）；JSON 解析容错增强（容忍代码围栏与包装对象——实测发现模型有时返回对象包装）；用作者 Key 做端到端冒烟测试：检索策略生成精准（如"第三章 方法设计→attention-based few-shot segmentation"）、推荐理由可用（如"作为注意力机制对抗网络的对比baseline→第四章"）
- **决策记录**：推荐系统的可用性标准确立——"相关"不够，必须"可解释"（每条推荐带可印证点），这也成为产品的差异化能力
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-16 · v2.8 双语检索与文献功能重分布（第 15 次会话）

- **人的工作**：三个问题——①检索出的标题都是英文，是否只有英文文献？②工作台文献检索逻辑要跟着优化？③点击查找后 UI 混乱，要求按"最方便最顺手"重新设计文献功能的交互与功能分布
- **AI 的工作**：v2.8——①新增 OpenAlex 中文文献通道（language:zh，实测返回真实中文文献，如《从U-Net到Transformer：混合模型在医学图像分割中的应用进展》），批量推荐与含中文的单次检索并行跑中英两组，按「🇨🇳中文文献/🌐英文文献」分组、中文排前、条目加"中文"徽标；知网无开放 API 的缺口由「AI 解析」粘贴入口补位（如实告知用户）；②文献功能按"收集→使用"重分布：文献中心（智能推荐置顶为主入口→AI解析/手动录入→文献库），写作工作台右栏精简为三件套（插入引用/摘要/素材）+「找文献→文献中心」按钮；③PRD 升级 v2.8（变更记录第 14 条）
- **决策记录**：工作台只保留"用文献"（插入引用），"找文献"（检索/批量/解析）全部归文献中心——按使用时机切分功能位置
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-16 · v2.9 完整论文文档与规范引用（第 16 次会话）

- **人的工作**：三个深刻的产品问题——①引用插入方式错误：完整 GB/T 7714 条目不应插正文，正文只应有序号，完整条目在文末参考文献统一列出；②章节各自写完没有全文汇总视图；③建议"工作台包含完整论文结构，左栏目录点击索引到合适位置"，并追问排版格式方案
- **AI 的工作**：v2.9 重构——工作台升级为一份连续完整的论文文档（题目/摘要/关键词/各章/参考文献/致谢），左栏目录点击定位（字符偏移 setSelectionRange，不重建 DOM 不丢光标）；保存时按标题行解析同步章节结构与摘要/关键词/致谢（buildDocument/parseDocument 往返测试通过）；引用改为规范模式：正文光标处只插 [n] 序号，文献库条目入库分配稳定编号 litNo（旧数据自动迁移），「复制全文/排版预览」按正文引用序号自动生成文末参考文献列表；排版预览=打印级 HTML（宋体、首行缩进、居中标题，可另存 PDF），纯文本编辑器不支持富文本排版的局限以"结构化文本+打印预览"方案解决；PRD 升级 v2.9（变更记录第 15 条）
- **决策记录**：排版方案——编辑器保持结构化纯文本（贴 Word/LaTeX 无障碍），排版通过"排版预览"打印级 HTML 实现，不做伪富文本
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-17 · UX 系统性优化第一轮（第 17 次会话，自主 UX 模式）

- **人的工作**：给出总目标——以职业 PM/UX 身份、站在典型用户处境系统走查应用，优化体验与交付表现，不限轮次自主推进
- **AI 的工作**（以"王姐从打开到写完"完整旅程走查后实施）：
  - 论文主页：新增「写作之旅」五步流程条（填Key/定题/采大纲/设截止/开始写，自动打勾、点击直达、当前步高亮标"下一步"），hero 主按钮恒指下一步，移除冗长的 5 条使用提示卡；空状态组件化（图标+说明+行动按钮）
  - 写作工作台：工具栏分组（初稿｜AI动作｜导出，视觉分隔线）；目录点击定位增加闪烁反馈；摘要支持「本章/全文」两口径；编辑器底部加操作提示（选中用AI动作、Ctrl+Z撤销）；参考文献占位文案精简
  - 计划与进度：甘特图顶部"今天"信息条 + 当前所处阶段橙色描边与「进行中」标签
  - 文献中心：入库成功后引导条「去写作工作台插入引用 →」
  - 设置：新增「🧪 演示数据一键载入」（示例论文含题目/5章大纲/2章草稿/3条编号文献/3天打卡，冒烟测试通过）——大幅提升课堂演示表现
  - 全局：滚动条美化、按钮/焦点/动效细节统一、顶部论文徽标悬停显示全称
  - PRD 升级 v2.10（变更记录第 16 条）
- **决策记录**：onboarding 用"流程条+下一步主按钮"替代"使用说明卡"；演示数据作为交付表现的一等公民（课程演示场景）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-17 · UI 整体重设计：纸墨朱砂视觉体系（第 18 次会话，自主设计模式）

- **人的工作**：授权"不限轮次自主优化，直到产出可展示的作品再提醒"；明确提出"UI 整体重新设计，运用 UI 相关技能整体提升"
- **AI 的工作**（应用 frontend-design 技能两轮流程——头脑风暴→对照 AI 模板三大俗批判修订）：
  - 确立「纸墨朱砂」设计语言：从论文写作的器物世界取材（稿纸、宋体铅字、朱批、印章、[n] 编号）。色彩=纸色底 #F6F5F1 / 墨色正文 #26303B / 墨蓝主按钮 #2F4F66 / 朱砂 #C03B2D 专用于 AI 动作与批注 / 松烟绿 #477A56 完成态 / 深墨侧栏 #1E2B36；标题与品牌用宋体族、数字/日期/DOI/字数用等宽字体——颜色与字体都承载语义而非装饰
  - 签名元素「朱批系统」：所有 AI 动作按钮朱砂描边（生成初稿/生成选题/生成大纲/批量推荐=朱砂实底），AI 结果框生成后亮红批注轨，学术诚信提示=红边注；完成态=印章式标记（已入库/已采用/已打卡/已完成）；写作编辑器=稿纸页（左红边线+淡网格+宋体+2 倍行距）
  - 全站实施：index.html 新外壳（印章 logo「文」+宋体 wordmark、状态徽标红绿点）、css/style.css 全量重写（约 500 行 token 体系）、五个模块视图模板全部适配（emoji 图标替换为线性 SVG 导航图标、卡片标题统一红方块标、文献条目 [n] 等宽编号 chip、GB/T 7714 条目宋体展示）、排版预览同步重设计（宋体论文版式+页脚诚信声明）
  - 修复存量 bug：demo-data.js 的 hasExistingData 未导入 getProject 导致设置页整页渲染失败（此前一直被 try/catch 吞掉显示"页面加载失败"）；写作防抖保存在模块卸载后空引用报错
  - 质量验证（Playwright 全流程）：31 项功能断言 + 19 项设计 token 计算样式断言 + 空状态 9 项 + 像素抽样（侧栏深墨/纸色底/白卡片）+ 排版预览弹窗（参考文献自动列出），全部通过、零控制台错误；桌面 1440px 与移动端 390px 均无横向溢出
  - 交付表现：设置页「载入演示数据」一键呈现完整论文状态，六视图截图存档
- **决策记录**：拒绝"米色+陶土红衬线""黑底+荧光点缀"两个 AI 模板俗套——红色只在"AI/批注"语义出现，用户动作统一墨蓝，完成态统一松烟绿；等宽字体只用于数字与数据（日期、[n]、字数、DOI），避免全场等宽的机械感
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-17 · 体验优化专项第一轮：审计→修复→增强→备份（第 19 次会话，执行 GOAL.md）

- **人的工作**：立 GOAL（执行版在根目录 GOAL.md）——功能设计与 UX 交互「不顺手、不成熟」，起 goal 持续优化直到成熟商业化产品；明确边界调整：允许新增功能模块（提案拍板制）与板块重构、不引后端、合规红线不动
- **AI 的工作**：
  - 体验审计：以核心画像走查五任务链×六视图，产出 16 项缺陷（P0×2 / P1×7 / P2×7，含三类历史遗留 bug）
  - P0/P1 修复：①AI 请求无超时 → 180s 超时保护（连接测试 30s）②清除数据遗漏 project/drafts → 全量清除并复位表单 ③AI 动作（润色/降重）无选区时改写全文 → 改为只作用于当前章节（防误操作）④手动录入/AI 解析成功后整页重渲染抹掉结果 → 局部刷新保留结果 ⑤文献入库 onDone 整页重渲染 → 只刷文献库 ⑥删除文献用数组索引定位（unshift 后索引错位会删错条目）→ 改用稳定编号 litNo 定位 + 被引用次数警告 ⑦设为题目不回填大纲表单 → 自动回填 ⑧采用大纲无下一步 → 原地追加「去写作」引导 ⑨关页 400ms 防抖窗口内容丢失 → beforeunload 兜底保存 ⑩`export...from` 不建立本地绑定 → formatCitation 未定义，手动录入与 AI 解析此前实际不可用（历史 bug，冒烟测试未覆盖真交互）
  - 板块增强：打卡日历热力图（70 天，绿格=已打卡/红框=今天）；移动端章节目录横排滑动；空态主页「载入演示数据看效果」一键入口；右栏引用「已引用×N」实时标记（随保存刷新）；已完成章页眉印章；初稿面板诚信提示；DeepSeek Key 申请链接；主页「继续写」智能指向未完成章
  - 新功能（作者拍板）：数据备份——导出全部本地数据为 JSON 文件（文件名含日期）、导入校验/覆盖确认/恢复后回论文主页；备份含 API Key 已提示勿外传
  - 质量验证：Playwright 回归扩至 51 项断言（新增清除残留、删除引用警告、局部刷新证明、热力图、备份导出/导入、智能继续写等），四套件（功能/样式 token/空态/像素+预览弹窗）全过、零控制台错误
- **决策记录**：修复优先于新功能——先消灭误操作、丢数据、死路，再做增量；备份功能走「提案→拍板→实施」流程（GOAL 范围管理实践）；回归断言随修复同步扩充，测试先行发现两个真实历史 bug
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-18 · 体验优化专项第二轮：编辑器交互与状态一致性（第 20 次会话）

- **人的工作**：要求"继续下一轮优化，继续打磨功能，让整体更顺畅更合理"
- **AI 的工作**（走查产出 9 项清单，全部实施）：
  - 编辑器交互（落实用户偏好的 Notion AI 式模型）：①选中文字后编辑器上方浮现「选区快捷操作条」（已选中 N 字 + 润色/扩写/降重，取消选区自动隐藏，动作直接作用于所选文字）②「标记本章完成」从左栏底部移到页眉行——写完就地打勾，已完成章显示印章且按钮隐藏 ③Ctrl/Cmd+S 立即保存（拦截浏览器默认行为 + 状态指示刷新）④生成初稿面板打开即聚焦要点输入
  - 状态一致性：⑤文献入库后搜索结果列表即时刷新（已入库条目置灰 + 「已入库」印章，不再停留在勾选态）⑥手动录入保存后字段自动清空（支持连续录入）
  - 错误路径：⑦AI 返回格式异常导致大纲解析出 0 章时明确报错并引导重生成（此前会提示"已采用大纲（0 章）"）
  - 连贯性：⑧主页打卡卡片复用 70 天热力图（组件抽至 ui.js calGridHtml，主页/计划页共用）⑨浏览器标签页标题随模块动态变化
  - 质量验证：回归扩至 56 项断言（新增选区条浮现/隐藏、页眉完成流、录入清空、主页热力图、动态标题），四套件全过、零控制台错误
- **决策记录**：选区条用「固定位置浮现」而非浮动跟随气泡——移动端长按选中同样可用，实现简单可靠；热力图抽共用组件避免两处复制维护
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-18 · 体验优化专项第三轮：撤销修复与演示友好性（第 21 次会话）

- **人的工作**：要求"继续下一轮"并报告 bug——输入框的撤销功能不生效
- **AI 的工作**：
  - 撤销 bug 定位（Playwright 探查）：合成键盘事件不触发浏览器级快捷键，故用 execCommand('undo') 对照——打字撤销本身正常，真正的缺陷是 AI 动作/插入引用走 setRangeText 路径，该编辑不进浏览器原生撤销栈，而 toast 却承诺"Ctrl+Z 可撤销"。修复：所有程序化编辑（AI 润色/扩写/降重/续写、生成初稿、插入引用）统一改为 execCommand('insertText') 以「用户编辑」写入——进入原生撤销栈，Ctrl+Z/Ctrl+Shift+Z 全链路可用；三路径（AI 编辑/打字/引用插入）撤销实测通过
  - 演示友好性（课堂演示与首次体验）：①AI 解析与选题建议各加「填入示例试试」按钮（一键填入真实示例，点生成即可演示）②设置页载入演示数据后自动跳回论文主页（此前只 toast）③生成初稿的替换确认弹窗显示本章现有字数（约 N 字）
  - 完成反馈：写作之旅五步全部完成时主页 hero 徽章行显示「旅程五步全部完成」
  - 质量验证：回归扩至 61 项断言（新增撤销恢复、示例填入×2、旅程徽章、设置页演示跳转），四套件全过、零控制台错误
- **决策记录**：撤销方案选「原生撤销栈」而非自定义撤销栈——insertText 用户编辑让 undo/redo 走浏览器原生机制，行为与任何编辑器一致、无需维护第二套栈；execCommand 虽已标记废弃但 insertText 是现行推荐替代缺失的公认方案
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-18 · 体验优化专项第四轮：中断恢复与按钮状态机（第 22 次会话）

- **人的工作**：要求"继续打磨优化整体的用户体验"
- **AI 的工作**（走查产出 4 项，全部实施）：
  - 中断恢复（P0 级缺陷）：AI 请求进行中切换页面时，结果会写入已被卸载的编辑器——此前静默丢失且 toast 虚假提示"已应用"。修复分三层：①写作模块建模块级 AbortController，导航事件（tm:navigate）时取消所有进行中的 AI 请求（润色/扩写/降重/续写、生成初稿、本章/全文摘要），取消后静默不打扰 ②响应到达时页面已离开 → 明确提示"AI 已完成，但页面已离开，未写入编辑器"（isConnected 防御，不再谎报成功）③排查发现事件总线 bug：tm:navigate 由 document.dispatchEvent 触发且不冒泡，但写作模块监听挂在 window 上——事件永远到不了；且侧栏导航点击直接调用 switchModule 不触发该事件。修复：监听统一挂 document；导航按钮（含设置）改走 tm:navigate 事件（与页内按钮一致）
  - 摘要按钮状态机（点必有应）：未生成时「保存为素材/复制」置灰（此前可点但点了报错）；生成中两个摘要按钮禁用（此前可连点导致双请求互相覆盖结果框）
  - 打卡按钮 disabled 说明：已打卡后按钮带 title 提示"今天已经打过卡，明天再来"
  - 移动端布局 bug（真实缺陷）：390px 下写作工作台横向溢出——grid 单列 `1fr` 的隐式最小轨道=min-content，页眉长文本（flex 不换行）与右栏 select 最长选项把轨道撑到 985px，选区条被推到视口外。修复：`minmax(0, 1fr)` + 三栏 min-width:0 + 长文本允许换行 + select 限宽
  - 质量验证：新增 Round 4 套件 19 项断言（fetch mock 模拟中断/竞态/慢响应、按钮状态机、移动端写作全旅程打字→选区→切章→打卡）；全量 108 项断言四套件全过、零控制台错误
- **决策记录**：中断恢复用「模块级 AbortController + 导航事件取消」而非「卸载时清理」——导航事件在模块渲染前触发，取消先于 DOM 替换，杜绝竞态；验证用页面侧 fetch mock（add_init_script 注入）替代 route 拦截——sync 版 Playwright 不调度 async route handler，且 mock 不监听 signal 可精确模拟「响应与切页竞态」
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-18 · 体验优化专项第五轮：选题页状态机与文献中断恢复（第 23 次会话）

- **人的工作**：要求"继续打磨优化整体的用户体验"；确认本轮跳过真机验收（Key 留在作者手中自测），AI 继续无 Key 走查
- **AI 的工作**（走查选题页 topic.js + 文献检索 litsearch.js，产出 4 项缺陷，全部实施）：
  - 选题页按钮状态机（点必有应，与摘要按钮一致）：未生成时「复制结果/复制/采用此大纲」置灰（此前可点但点了报错"请先生成…"）；生成成功后启用
  - 采用流程缺陷（真实缺陷）：「采用此大纲」点击后按钮变印章，但若此后重新生成大纲，印章永久挡路——没有入口再次采用新大纲，采用流程断链。修复：重新生成大纲时自动清理上次的印章与「去写作工作台写第一章 →」引导，重建「采用此大纲」按钮，采用→重生成→再采用闭环可无限循环
  - 文献检索中断恢复（与写作模块一致）：此前批量推荐（AI 检索策略→多组真实库检索→AI 推荐理由标注）进行中切页时，请求全部跑完——浪费 token、结果写进已卸载 DOM。修复：litsearch.js 建模块级 AbortController，随 tm:navigate 取消；fetch（CrossRef/OpenAlex）与 chat（策略/标注）全部透传 signal；aborted 静默不打扰，其余失败正常报错；单次检索路径同步覆盖
  - 文献勾选热区：勾选框原来只有 15px 的小方框可点——改为 label 包裹整条（checkbox + 标题 + 理由 + 元信息），点击标题行任意位置切换勾选；已入库条目 label 提示"已入库"
  - 质量验证：新增 Round 5 套件 26 项断言（三按钮状态机、采用→重生成→再采用闭环、批量推荐中断后不再发出标注请求、策略请求不重复、点击标题行切换勾选、入库置灰印章、全程零 console 错误）；全量 108 项断言四套件 + 移动端 390px 无溢出全部通过
- **决策记录**：中断恢复方案跨模块复用（写作/文献两个模块级 controller，命名区分 __tmWritingAbort / __tmLitAbort，避免共享状态耦合）；fetch 层的取消抛 DOMException('AbortError')、chat 层抛 {code:'aborted'}，统一用 isAbort(e) 双判；label 包裹列表项用 flex（.lit-cb）保持原 checkbox 视觉位置，div→span 后补 display:block 保证 margin/背景正常
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 真机验收清单（作者自测 · 需在「设置」填入自己的 DeepSeek Key）

按五条任务链 + 数据安全逐项走查，对照 GOAL.md 验收标准（KR1-KR6）；每项预期结果如下：

**① 首次配置**：设置页填 Key →「测试连接」提示成功；「载入演示数据」后自动回论文主页
**② 选题采用链**：生成选题建议 → 5 个候选卡片（可行性/创新点）→「设为论文题目」→ 印章 + 右侧题目自动回填 →「生成研究框架」→「采用此大纲」→ 印章「已采用 · 5 章」+ 写作引导 → **再点一次「生成研究框架」，确认采用按钮恢复、印章与引导清理（第五轮修复点）**；未生成时复制/采用按钮应为置灰
**③ 写作链**：写一段文字 → 选中 → 快捷操作条浮现 → 润色/扩写/降重 → 就地替换 + Ctrl+Z 可撤销 →「生成初稿」确认替换（含字数）→ 页眉「标记本章完成」→ 摘要生成 → 保存为素材；**AI 请求进行中切页应无假提示、无报错**
**④ 文献链**：批量推荐 → 检索策略 → 真实文献（分组/DOI/原文）→ 每条带推荐理由 → 点标题行任意处切换勾选（第五轮修复点）→ 入库 → 印章 + 「去写作工作台插入引用 →」→ 正文 [n] → 复制全文/排版预览文末自动生成 GB/T 7714 参考文献；**检索进行中切页应静默取消、无报错**
**⑤ 计划链**：设截止 → 甘特图 → 打卡 → 热力图绿格 + 按钮置灰提示；主页五步旅程完成出徽章
**⑥ 数据安全**：导出备份 JSON → 修改数据 → 导入恢复；**备份文件含 API Key，勿外传**

## 2026-08-18 · 体验优化专项第六轮：文献中心与设置页状态一致性（第 24 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（走查文献中心 citation.js + 设置页 settings.js，产出 3 项缺陷 + 1 个回归发现的深层 bug，全部修复）：
  - AI 解析中断恢复（与写作/文献模块一致）：此前「解析并保存」进行中切页，请求照跑——结果写进已卸载页面且 toast 提示已保存。修复：共享 AbortController（`__tmAbort`，幂等初始化——与 __tmWritingAbort/__tmLitAbort 并存），chat 传 signal；**aborted 语义=不入库不打扰**（导航取消即放弃操作）
  - 连接测试中断恢复：testConnection 支持外部 signal（api.js），设置页「测试连接」切页静默取消
  - 手动录入实时预览（真实缺陷，desc 承诺与行为不符）：页面文案写「规则引擎实时生成 GB/T 7714 格式」，实际只有点「生成并保存」才出预览。修复：六个字段 input 事件防抖 300ms 实时生成格式；题名清空时回占位提示
  - 防抖竞态深层 bug（回归发现）：保存后清空字段的动作与待触发的防抖 timer 竞争——timer 以空表单覆盖刚生成的预览（首次修复只加 updatePreview() 同步刷新，回归仍失败；根因是清空后 timer 触发）。修复：清空字段后 clearTimeout(previewTimer) 取消待触发更新
  - 质量验证：新增 Round 6 套件 21 项断言（实时预览触发/更新/清空回位、保存后预览保留与字段清空、解析入库、**解析中断后不入库且请求仅一次**、测试连接中断静默、测试成功显示毫秒）；全量 155 项断言七套件全过、零 console 错误
- **决策记录**：中断取消的语义按操作类型区分——写作编辑类「取消即放弃」（不入库/不写入），数据持久化类同样「取消即放弃」（解析中断不入库），避免用户以为已保存；防抖与程序化清空并存时必须显式取消 timer，这是防抖组件与「保存后清空」交互的通用陷阱
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-18 · 体验优化专项第七轮：主页卡片图标统一与状态提示补全（第 25 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（走查论文主页 dashboard.js，产出 3 项，全部实施）：
  - 图标体系统一（v3.0 遗留不一致）：v3.0 重设计时侧栏导航已换线性 SVG，但主页四个功能卡片仍用 emoji（🧭✍️📚📅）。修复：线性图标提取为共享模块 js/icons.js（feather 风格 1.5px 描边 currentColor），app.js 侧栏与 dashboard 卡片共用——图标渲染单一来源，后续任何模块加卡片自动一致
  - 距截止天数 danger 提示：≤14 天时红色数字增加 title 提示「已不足两周，注意写作节奏」（此前红色无解释）
  - 写作之旅步骤 title 提示：悬停显示步骤状态（已完成/当前步骤）
  - 质量验证：新增 Round 7 断言（主页卡片 4 个 SVG 图标、侧栏 6 个 SVG 图标不回归、旅程 title、danger 提示、旅程进度 4/5 状态正确性）；全量回归 155 项断言七套件全过、零 console 错误
- **决策记录**：视觉统一是「成熟产品」观感的关键项——同类元素（导航/卡片入口）必须同源渲染，提取共享图标模块避免各模块各写一套
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-18 · 体验优化专项第八轮：计划与进度模块走查（第 26 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（走查 planner.js 计划与进度——五条任务链中最后一条单独走查的模块：甘特图倒排、打卡流、截止联动；产出 6 项，全部实施）：
  - 甘特图已完成阶段标记（KR6 成必有标记）：此前甘特图只有「进行中」朱砂描边，阶段结束无任何完成反馈。修复：`s.end < now` 判定 done → bar 松烟绿 + 标签「✓ 已完成」chip + title 补充；信息行改为「朱砂描边＝当前阶段 · 松烟绿＝已完成阶段」
  - 甘特图章节进度集成：撰写阶段挂的任务 chips 原本是静态章节名，看不出进度。修复：chips 按 chapterProgress 显示状态——已完成章「✓ 章名 · 已完成」（done chip）、进行中章「章名 · 进行中」（doing chip）——计划页真正同时看到「计划」与「进度」
  - 计划模板补齐 4 种学位：此前只有本科 12 周/硕士 24 周，选题页支持 4 种学位而博士/课程论文无对应模板。修复：新增博士毕业论文（36 周）、课程论文（8 周），模板下拉 4 种
  - 甘特图溢出保护：`Math.max(6,…)` 最小宽度 clamp 使短阶段（1 周）占比放大，多阶段总和 >100%，bar 溢出 track（本科模板实测 ~103.6%）。修复：.gantt-track 加 overflow:hidden
  - 打卡预选当前章：工作台正在写的章节（currentChapter）自动选中为「今天写了哪一章」默认项，减少一步操作
  - 过截止文案矛盾：此前显示「距截止 0 天」，与主页「已过截止 X 天」不一致。修复：desc 改为「已过截止 X 天」
  - 质量验证：新增 Round 8 套件 22 项断言（完成/当前阶段标记、信息行、章节 chips 三态、过截止文案、打卡预选、模板 4 种、生成计划流含截止同步与章节挂载、打卡流、track 溢出隐藏、空态回位）；全量回归 147 项断言八套件全过、零 console 错误
- **决策记录**：甘特图是「计划+进度」双职责视图——进度信息要嵌进计划本身（完成态染色 + 章节状态 chips），而非另开区域展示；模板按学位类型对齐（选题页 4 种学位 ↔ 计划模板 4 种），避免用户选了博士论文却只能套本科模板
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第九轮：选题与大纲模块走查（第 27 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（走查 topic.js 全文——选题链剩余未深入区域，产出 3 项，全部实施）：
  - **选题/大纲 AI 调用中断恢复（P1，五条任务链最后一块拼图）**：此前 writing/litsearch/citation/settings 四模块均已支持「导航切页即取消 AI 请求」，唯独选题页的「生成选题建议」与「生成研究框架」不带 signal——请求照跑、结果写进已卸载页面、白耗 token。修复：模块级 `__tmTopicAbort`（与各模块同构：document 监听 tm:navigate 换新 controller），两个 chat 调用透传 signal，catch 中 aborted 静默放弃。至此五条任务链的 AI 调用点全部具备中断恢复
  - 学位类型回显（P2，真实缺陷）：`#outline-degree` 无回显逻辑，重新进入页面/换了论文后回退默认「本科论文」，生成大纲会错套模板（此前在选题页设过博士论文、下次生成却按本科结构出大纲）。修复：按 p.degreeType 精确回显，4 种学位选项动态生成
  - 生成中 filled 态残留（P2）：重新生成时结果框带成功态高亮边框显示占位文案。修复：填充占位前 remove('filled')
  - 质量验证：新增 Round 9 套件 16 项断言（学位回显、生成中无 filled 高亮、**选题与大纲两条 AI 链中断后静默——无结果写入/复制与采用按钮保持禁用/零报错**、正常流回归：选题卡片→复制→设为题目→印章→题目同步与入库、大纲生成→采用→印章+引导+3 章入库）；全量回归 163 项断言九套件全过、零 console 错误
- **决策记录**：中断恢复已是全站统一模式（五模块同构：幂等 controller + document 级 tm:navigate 监听 + aborted 静默），本轮补上最后一块——审计时可以按「模块内是否有 fetch/chat 调用」清单核对覆盖度；学位类型回显说明「表单默认值必须回显已存数据」是状态一致性的基础项，所有带数据回显能力的表单都要过一遍
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第十轮：写作工作台全文走查（第 28 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（走查 writing.js 全文——核心模块此前只修过具体缺陷、未全文走查；产出 4 项，全部实施）：
  - **整章替换数据破坏缺陷（P1，自动化复现确认，最高严重级）**：「未选中」状态下点润色/扩写/降重、以及「生成初稿」，替换区间是 `findChapterRange` 返回的整章范围——start 含章节标题行、end 含章尾全部换行。两个后果叠加成结构性数据破坏：(1) AI 输出不含章节标题，替换把标题行也删掉；(2) AI 输出不保证以换行结尾，替换后章节内容与下一区块标题粘连成一行，`parseDocument` 按行解析时丢失下一章。**复现结果：3 章大纲在防抖保存后只剩 1 章，outline 被覆盖销毁**。修复：新增 `chapterBodyRange()`——替换区间改为标题行之后的正文区（标题行保留），整章替换统一 `reply.trimEnd() + '\n'` 补章尾换行；AI 上下文仍含标题行（帮助理解章节）。修复后复现验证：3 章全保留、无粘连
  - 空回复防御（P2）：AI 返回空内容时静默替换为空（等于删除选中内容）。修复：空回复 toast「AI 返回内容为空，请重试」不写入
  - 素材复制换行丢失（P2）：复制按钮把素材内容放 HTML dataset 属性，多行内容被 HTML 属性解析规范化为空格。修复：闭包数组读取原文
  - 素材截断提示（P2）：素材列表 slice(0,10) 无声截断。修复：超过 10 条显示「素材共 N 条，显示最近 10 条」
  - 质量验证：新增 Round 10 套件 14 项断言（**整章润色/初稿替换后大纲 3 章全保留+标题行保留**、选区替换只改选中部分、续写后结构不变、空回复不写入、素材复制保留换行、素材截断提示、摘要生成→保存素材链）；全量回归 177 项断言十套件全过、零 console 错误
- **决策记录**：整章替换这类「范围替换」操作必须区分「标题行」与「正文区」——替换语义是「AI 输出替换正文」，标题行是文档结构锚点，绝不能被替换内容波及；AI 输出文本不能假设尾随换行，任何「替换到区块边界」的写入都要显式保证边界分隔。此缺陷说明：走查不能只看交互，要按「数据流」追踪——编辑器文本 → parseDocument → saveProject 的完整链路上每一步都可能破坏结构
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第十一轮：设置页与数据模型走查（第 29 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（走查 settings.js 全文 + project.js 数据模型——前者 R6 只查过连接测试、备份链路未验证；后者是全部数据的源头；产出 4 项，全部实施）：
  - **连续打卡时区错位（P2，project.js calcStreak）**：原实现 `Math.floor(Date.now()/DAY)` 取 UTC 天序号再转本地日期，在 UTC+8 且 UTC 日尚未切换时（本地午夜后）起点日期比本地早一天——今天打的卡被跳过起点判断，连续天数算错。修复：全部改用本地日期（`isoLocal(Date.now())` 起步、逐日 -DAY 回退），语义保持「今天未打卡从昨天起算」
  - 表单注入面（P2，settings.js）：API Key/模型名/自定义 Base URL/论文题目的 value 回显未 escapeHtml，恶意输入可逃逸属性注入 HTML；「测试连接」的模型回复直接 innerHTML 未转义（模型输出可注入 `<b>` 等任意 HTML）。修复：五处全部 escapeHtml（Key 是密码框同样存在 value 属性注入面）
  - 清除数据清单硬编码（P3）：cfg-clear 只删 6 个硬编码 key，未来新增的 paperpilot:* 数据会残留。修复：与导出对称——遍历 localStorage 清除全部 paperpilot: 前缀
  - 质量验证：新增 Round 11 套件 15 项断言（calcStreak 五场景：连续/从昨天起算/断开/空/乱序；四个输入框注入渲染无元素且值原样；测试回复按字面显示无 HTML 解析；清除数据前缀全清+非前缀保留；**导出→清空→导入恢复完整链路**）；全量回归 192 项断言十一套件全过、零 console 错误
- **决策记录**：凡「日期参与计算」一律用本地日期字符串流转（打卡数据存的就是本地日期，比较也必须是本地）；凡「动态文本进 innerHTML」必须 escapeHtml——模型输出、表单回显、用户数据三类来源全部走转义，这是纯静态无后端应用的唯一防线
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第十二轮：文献中心库管理走查（第 30 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（走查 citation.js 全文——文献链「收集→使用」的收口端（库管理），此前 R6 修过解析中断/实时预览，编号分配、删除衔接、解析防御未查；产出 4 项，全部实施）：
  - **补号冲突缺陷（P2）**：`ensureNumbers` 用数组下标 i+1 补号——旧数据有编号 5、两个缺号条目补成 1、2，与已有编号 1 冲突，出现两条 [1]（正文引用 [1] 时解析错误、复制全部输出重复编号）。修复：按 `max(litNo)+1` 起递增分配，冲突测试断言编号唯一
  - **AI 解析非对象防御（P2）**：`entries.forEach` 直接按元素取字段——AI 返回含字符串元素（`["张伟...", {...}]`）时字符串的 `.type`/`.title` 为 undefined，入库脏条目、列表渲染 `escapeHtml(undefined)` 输出 "undefined"。修复：过滤 `e && typeof e === 'object'`，只入库对象元素；全非法时提示「AI 未解析出文献条目」不入库
  - **空数组提示（P2）**：AI 返回 `[]` 时走 forEach 空循环，显示「已解析 0 条并保存」的假成功 + 空白 filled 框。修复：objs 为空时明确错误提示 + 错误 toast，不再假装成功
  - **解析中 filled 残留（P3）**：重新解析时成功态高亮边框残留。修复：解析前 remove('filled')（与 topic 第九轮同源问题，库管理模块漏掉）
  - 质量验证：新增 Round 12 套件 19 项断言（补号 max+1 唯一不冲突、解析流含 filled 清理窗口、只入库对象元素、空数组明确提示且不入库、**删除引用过文献弹警告/未引用不警告**、手动录入实时预览→保存清空→预览保留→在库；全程零 console 错误）；全量回归 211 项断言十二套件全过
- **决策记录**：编号是「正文引用 [n]」与「文献库条目」之间的稳定键——任何重新编号都会悄悄破坏正文引用，所以补号只能递增分配、删除不做重排（位次留空，靠排序容错）；「AI 输出不可信」原则在解析场景的落地：数组内容逐个类型校验、空结果显式报错而非假装成功——AI 集成代码的默认假设应是输出可能含任何东西
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第十三轮：共享基础设施走查（第 31 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（走查 api.js + storage.js + ui.js + gbt7714.js——四个被所有模块依赖的共享层，此前从未整体走查；产出 5 项，全部实施）：
  - **响应解析无兜底（P2，api.js）**：服务商返回 200 但 body 非 JSON（网关/代理 HTML 页）时 `res.json()` 抛原始 `SyntaxError`，模块错误框显示「Unexpected token < in JSON」这类不可读的英文报错。修复：try/catch 包 json()，抛 `bad_response`「服务商返回了无法解析的响应，请稍后重试」
  - **加载态原文覆盖（P2，ui.js setLoading）**：连续两次 `setLoading(btn, true)` 时第二次把「生成中…」存进 `data.orig`，结束时按钮永久卡在加载文案恢复不了原文。修复：orig 只在首次进入 loading 时保存，恢复后释放
  - **403 未映射（P3，api.js）**：403（Key 无权限/被停用）落进兜底「请求失败（HTTP 403）」，误导排查方向。修复：映射为 auth「API Key 无权限或已被停用」
  - **存储容量触顶无兜底（P3，storage.js）**：数据增长触顶 localStorage（QuotaExceededError）时 `set` 直接抛未捕获错误，防抖保存路径崩溃且用户不知情。修复：set 内部 catch，明确 toast「本地存储空间不足：请先导出备份，再清除不需要的数据」+ console.error 留痕
  - **toast 挂载防御（P3，ui.js）**：`#toast-wrap` 不存在时 null.appendChild 崩溃。修复：缺失时静默返回
  - 质量验证：新增 Round 13 套件 15 项断言（非 JSON 响应可读错误、连续 loading 原文恢复、403 无权限提示、容量触顶 toast 弹出+零未捕获错误、**GB/T 7714 六种格式规则单元断言**（J/D/M/C/超3人取前三加等/无作者省略）、日历 70 格+今天标记+图例）；全量回归 226 项断言十三套件全过
- **决策记录**：共享层是「一次写错、全局踩雷」的位置——api.js 的错误文案会被五个模块原样展示，storage.js 的崩溃点在防抖保存里无声无息。走查顺序应优先共享层（被依赖方），再走各模块（依赖方）；「零 console 错误」验收口径：已处理失败的预期调试日志（console.error 留痕）不计入
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第十四轮：在线文献查找走查（第 32 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（走查 litsearch.js 全文——R5/R6 修过中断恢复与勾选热区，但全文未走查；同时审查 demo-data.js；产出 2 项修复，全部实施）：
  - **批量推荐标注阶段 abort 未接住（P1，unhandled rejection）**：`runBatch` 里 AI 标注推荐理由的 `await annotateCandidates(...).catch(e => { if (isAbort(e)) throw e; ... })` 位于 runBatch 的 try/catch **之外**——rethrow 的 abort（切页/超时/网络中断）变成 unhandled rejection（console error），且 `setLoading(batchBtn, false)` 不执行，按钮卡「AI 标注推荐理由中…」。对比：单次检索 `run()` 的同款调用在外层 try 内能接住，runBatch 是唯一漏点。修复：try/catch 就地接住 abort（setLoading 恢复 + return），非 abort 错误退回无理由结果（不影响已检索到的文献）
  - **数据库直连 res.json 无兜底（P2）**：searchCrossRef/searchOpenAlex/searchOpenAlexZh 三处 `res.json()` 在 200+非 JSON body（网关页/限流页）时抛原始 SyntaxError；`searchLiterature` 双源都失败时错误框显示「Unexpected token < in JSON」这类英文报错（与第十三轮 api.js 修复同类问题，直连 fetch 路径漏掉）。修复：三处统一 try/catch → 可读文案「XX 返回了无法解析的数据，请稍后再试」
  - demo-data.js 审查结论：无缺陷（演示文献 litNo 1-3 与草稿内 [1][2] 引用一致、checkins 结构与 calcStreak 输入格式匹配、覆盖确认在调用方 settings.js 已做）
  - 质量验证：新增 Round 14 套件 11 项断言（**标注阶段切页 abort 零 unhandled rejection**、非 JSON 响应可读错误且不显示 SyntaxError、批量推荐全流程：AI 检索策略→中英文双通道检索→分组渲染→默认勾选 3 条→入库→已入库印章→引导跳转写作工作台）；全量回归 237 项断言十四套件全过
- **决策记录**：「await X.catch(rethrow abort)」模式在三处出现（run 的 annotate / runBatch 的 annotate / runBatch 的检索）——只有 runBatch 的 annotate 在 try 外。教训：**catch 里 rethrow 的 abort 必须被外层接住，否则就是 unhandled rejection**；走查异步函数时逐个确认每个 await 的异常路径落在 try/catch 内或就地处理
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第十五轮：复制全文/排版预览交付链走查（第 33 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（走查写作工作台的「成品交付链」——buildRefList/fullText/openPrintPreview，v2.9 完整文档模型的收口端；产出 1 项修复，含两个调用点联动）：
  - **缺号引用静默丢弃（P2）**：正文引用了 [3] 但文献库没有 3 号条目时，buildRefList 把 [3] 静默过滤掉——复制全文/排版预览的参考文献列表缺一条，用户完全无感知，直到交论文才发现「引用 [3] 在文末没有对应条目」（学术规范硬伤）。修复：buildRefList 返回 {refs, missing} 双值；复制全文与排版预览点击时若存在 missing，toast 明确警告「正文引用了 [3]，文献库未找到」；排版预览页内追加缺号提示条（朱砂色警示，@media print 隐藏——屏幕自查不污染 PDF 成品）
  - 质量验证：新增 Round 15 套件 12 项断言（复制全文：列表含在库编号条目/不含缺号条目、缺号 toast 文案指明 [3]；排版预览：新窗口打开、参考文献渲染、缺号提示条显示、打印 CSS 隐藏规则存在；引用完整时无缺号警告；全程零 console 错误）；全量回归 249 项断言十五套件全过
- **决策记录**：「正文 [n] 引用 ↔ 文献库编号」的一致性校验是学术规范硬需求——交付环节必须显式核对并提示，宁可打扰不可静默。打印级输出里的自查提示用 @media print 隐藏，兼顾「打印前能自查」与「打印成品干净」两个目标
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第十六轮：KR1 首次闭环体验审计（第 34 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（按 GOAL 验收标准 KR1「新用户 10 分钟内独立完成首次写作闭环」做全流程审计：空数据全新用户视角，五步旅程 + hero 行动链 + 跨模块状态同步 + 设置页闭环引导；产出 2 项修复，其中 1 项为 P2 级数据保护）：
  - **设置页「回主页看下一步」引导（P1，衔接可见性）**：新用户填完 API Key 后停留在设置页，不知道下一步去哪——旅程第 1 步完成但无去向提示。修复：保存配置后出现「回论文主页看下一步 →」按钮（条件：已配 Key 且无论文题目；有题目后自动隐藏，G1 验证），点击回论文主页（旅程 step2 指向选题）。KR1 的「无卡点、无指点」从设置页到选题页的衔接闭环
  - **手动编辑吞并标题行导致部分丢章（P2，防误操作，R10 教训的手动路径变体）**：用户把光标点到章节标题行开头再按 Enter/打字时，「第二章 相关理论基础」被吞进上一行正文（isChapterLine 行首锚定失效）——parseDocument 解析出的章节从 3 章变 2 章，**防抖保存时 persistDocument 用「全部丢失才保护」的旧判定，部分丢失时静默用残缺大纲覆盖**（真实数据损失：3 章大纲变 2 章）。修复：persistDocument 增加 lost 数组判定——新旧章节名不完全相同且丢失数 >0 时，保留原大纲结构 + toast 朱砂警告「检测到章节「第二章 相关理论基础」疑似丢失，已保留原大纲结构」；新增章节（改名/拆分）场景不受影响（lost 为空仍正常覆盖）
  - 质量验证：新增 Round 16 套件 33 项断言（空数据主页五步旅程 0/5→5/5、hero 行动链按状态指向正确模块（无 Key→设置 / 无题目→选题 / 无截止→计划 / 齐备→继续写作）、API pill 未配置/已配置、题目徽标截断、论文基本信息跨模块同步（设置页改题目 → 主页/写作同步）、选题卡片生成与采用、大纲采用印章、甘特图生成、第一章正文写入→标记完成→主页「已完成 1 章」、设置页引导显示/隐藏、**标题行首插入验证：outline 3 章保持 + 结构保留 toast 弹出**、全程零 console 错误）；全量回归 **282 项断言十六套件全过**
- **决策记录**：R10 修的是「AI 整章替换吞标题行」的机器路径，本轮发现「手动编辑吞标题行」同样致命——结构保护必须覆盖两个写入路径。persistDocument 的「保护 vs 覆盖」判定从二元（全部丢失才保护）升级为三元（完整一致→覆盖 / 部分丢失→保护+提示 / 全部清空→覆盖），改名/拆分章节（lost 为空）不受保护误伤。测试套件教训：H 段插入目标章节名必须与 MOCK 大纲实际生成的名字一致（「第二章 相关理论基础」而非「第二章 方法」），否则 indexOf=-1 插入落点错误、吞标题场景根本没发生——测试本身先验证「场景成立」再验证「修复生效」
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第十七轮：移动端 390px 全量回归（第 35 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（按 GOAL 验收标准 KR4「零横向溢出（桌面+移动端）」与 KR5「390px 移动端核心旅程可完成」做移动端全量回归——上次移动端深测是第四轮（第 22 次会话），此后 13 轮改动（旅程条、选区工具条、回主页引导、缺号提示条等新元素）未再在 390px 验证；产出 0 项产品缺陷、1 项验证结论 + 3 项测试方法论修正）：
  - **验证结论：移动端六视图零横向溢出、核心旅程完整可完成**——390px 视口下走完六视图（主页/选题/写作/文献/计划/设置）+ 逐模块滚动检查 `scrollWidth - clientWidth === 0`（含 R4 修复的 grid 单列 minmax(0,1fr) 之后 13 轮的新元素：5 步旅程条、hero 按钮、选区快捷操作条（#wb-sel-bar 选中 4 字出现且左右边界在视口内）、章节导航 .chapter-item 点击切章（currentChapter 更新）、打卡按钮、回主页引导按钮 #cfg-back-home、Key/导出表单元素）；移动端写作旅程真实完成：打字 → 选区 → 切章 → 打卡 → 插入引用；长文案 toast（缺号引用警告「正文引用了 [2]，文献库未找到」、结构保护警告「检测到章节…疑似丢失」）在 390px 下均不横向溢出；全程零 console 错误。**KR4/KR5 移动端验收项成立**
  - 测试方法论修正 1（drafts 数据结构）：preset localStorage 的 drafts 值为 `{content, updatedAt}` 对象（persistDocument 保存时包装），写成纯字符串会导致 `drafts[c.chapter]?.content` 为 undefined、草稿静默不渲染——编辑器里章节标题「重复」的假象（content 误带标题行时标题渲染两遍）也由此而来
  - 测试方法论修正 2（execCommand 焦点依赖）：`document.execCommand('insertText')` 需要 textarea 持有焦点；模块重渲染（导航）后编辑器是新 DOM 无焦点，evaluate 里漏 `ta.focus()` 时插入静默失败——同一段代码在 focus 保持的上下文中「看起来通过」、重渲染后失败，断言必须用「插入后内容存在性 + 位置」双重校验（此前 C5 的 includes 检查是假通过）
  - 测试方法论修正 3（选择器类名）：旅程条是 `.journey-step`（非 journey-steps）、选区工具条是 `#wb-sel-bar`（非 .selbar）、章节导航是 `.chapter-item`（非 [data-chap]）、文献条目是 `.item`（非 .lit-item）——4 个失败全部由此，全部修正后 37/37
  - 质量验证：新增 Round 17 套件 37 项断言（六视图逐模块零溢出、关键元素可见可点、移动端写作旅程打字/选区/切章/打卡/引用、长 toast 不溢出、零 console 错误）；全量回归 **319 项断言十七套件全过**
- **决策记录**：移动端验证不能只做「载入 + 截图」——交互旅程（选区工具条、切章、打卡）和 toast 边界必须断言。headless 无法覆盖软键盘遮挡选区工具条的真实场景，列入真机验收清单（作者自测时重点看手机端选区工具条是否被键盘顶起）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第十八轮：跨模块数据一致性审计（第 36 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（按 GOAL 验收标准 KR2「五条任务链走查一次性完成率 100%」审计跨模块数据一致性——设置页是项目主线的编辑入口（project.js updateBasics → tm:project-changed → 主页徽标），审计「改一处 → 各模块同步/重渲染刷新」与「清空 → 全模块空态恢复」；产出 0 项产品缺陷、18 项断言验证一致性成立）：
  - **设置页改题目 → 跨模块同步（A 段）**：保存后主页徽标实时同步（tm:project-changed 事件链）、hero 区显示新题目、写作工作台文档首行（buildDocument 标题）重渲染后为新题目、选题页输入框保持「研究方向」语义不回显题目（设计确认：输入框是自由研究方向，论文题目在主页/设置/写作页展示）
  - **改截止日期/学位类型 → 计划与选题刷新（B/C 段）**：主页「距截止 N 天」刷新、计划页截止日期重渲染同步、选题页学位类型回显（大纲模板口径一致）
  - **清空数据 → 全模块空态恢复（D 段）**：主页徽标「未设置论文」+ 旅程复位 0/5、写作页「还没有大纲」引导、文献中心「暂无文献，去上方收集吧」、计划页空态、localStorage 前缀数据确实清零
  - **演示数据一键恢复（E 段）**：重载后徽标恢复 + 旅程 5/5（key+题目+大纲+截止+草稿全满）
  - 质量验证：新增 Round 18 套件 18 项断言（上述 A-F 全链路）；全量回归 **337 项断言十八套件全过**
- **决策记录**：纯静态架构的数据同步模式是「事件即时刷新（徽标/pill）+ 模块重渲染时读取最新数据（页面内容）」两层——审计确认此模式一致成立，无需中央 store。五个测试失败全部是断言设计错误而非产品缺陷（选题输入框语义、文献中心空态文案「暂无文献」vs 工作台右栏「文献库为空」、旅程步数计算 5/5 而非 3/5、placeholder 引导词）——**跨模块审计的断言必须逐个查实际文案与语义，不能凭印象写**
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第十九轮：性能审计（第 37 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（按成熟产品标准审计时间体验——KR4 覆盖「零渲染失败」但未覆盖「多快」；首轮量化基线，产出 0 项性能缺陷、10 项性能基线断言固化）：
  - **量化基线（headless Chrome，Mac 本机）**：首屏 load 79ms（全站 175KB 原生 ES Modules 无打包器，浏览器按需加载，最大单模块 writing.js 38KB）；六模块切换 3-19ms；**96K 字符（8 章 × 1.2 万字）超长文档**：防抖保存 2.5s 内完成且 8 章草稿全部落盘、追加 200 字打字 20ms 无卡顿、复制全文构建 312ms；365 天打卡热力图 217ms；甘特图生成 575ms；文献跨组去重 Set O(n)（无二次方路径）
  - **结论**：纯静态架构天然无性能瓶颈——无框架重渲染、无打包器加载链、localStorage 同步读写对小数据量即时。防抖保存（800ms 合并）与 O(n) 解析（parseDocument 逐行正则）在论文体量（≤10 万字）内不构成卡顿源
  - 质量验证：新增 Round 19 套件 10 项断言（首屏 <1.5s、模块切换最慢 <300ms、96K 字防抖保存 <6s + 8 章落盘、追加 200 字 <200ms、复制全文 <1.5s、365 天热力图 <1s、甘特图 <2s、零 console 错误）；阈值取宽松值防 CI/硬件抖动，数字本身在 dev-log 留档；全量回归 **347 项断言十九套件全过**
- **决策记录**：性能验收口径=「交互即时（打字/切换 <200ms）+ 重型操作（保存/复制 <2s）+ 数据上限（10 万字论文）」三档，写入断言套件作为回归防线；本机基线数字已留档，真机（低端手机）首屏与切换时间留待真机验收补充
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第二十轮：AI 提示词质量审计（第 38 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（按「AI 输出质量即产品力」标准审计全部 9 个 chat() 调用点——四动作（润色/扩写/降重/续写）的 prompt 此前只有所选文字、没有论文题目与章节上下文，AI 改写时「不知道在写什么论文」；产出 1 项 P2 修复 + 1 项 P2 行为观察、15 项断言）：
  - **四动作注入论文题目 + 章节上下文（P2 修复）**：新增 `ctxLine({title, chapter})` helper（`论文《标题》「章节」章节`,无题目时退化为「（未定题）」,无章节时省略）——润色/扩写/降重/续写四个 prompt 全部改为 `(t, ctx)` 签名,`runAction` 调用点注入 `{ title: getProject().title, chapter: current }`;此前 AI 只拿到 4 个字选区的文本,主题背景为零,续写/扩写容易跑偏方向
  - **顺带确认既有上下文完整性**：初稿（含题目+学位类型+字数要求）、摘要（250-400 字+关键词格式）、选题（专业方向+可行性）、大纲（题目+学位）、文献解析（GB/T 7714+JSON 约束）五个调用点上下文均已完整,无需改动
  - **P2 行为观察（不修,记录）**：编辑器首行即题目行（buildDocument）,选中题目行文字做 AI 动作时,persistDocument 会把 AI 改写后的首行提取写回 project.title——项目题目被静默改写。行为链一致且可解释（改题目行自然改题目）,但「AI 静默改题目」易困惑;后续轮次若做防误操作可加 toast 提示
  - 质量验证：新增 Round 20 套件 15 项断言（四动作 prompt 含题目/章节/续写原文片段保留、无题目退化「未定题」、其余五调用点上下文完整、全程零 console 错误）;全量回归 **362 项断言二十套件全过**
- **决策记录**：prompt 断言的关键是 mock 关键字与真实 prompt 逐字核对（本轮发现降重 mock 关键字「重新组织以下文字的语言表达」与实际 prompt「重新组织语言表达」不一致,已对齐）;测试种子必须避开题目行与正文的重复词（indexOf 命中题目行会污染标题 → lastIndexOf 命中正文）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第二十一轮：KR1 首次闭环端到端计时走查（第 39 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（GOAL 第一条验收标准 KR1「新用户 10 分钟独立完成首次写作闭环」此前只有引导存在性审计（R16），没有端到端计时验收——本轮从「已配 Key、无题目、无文档」空数据起点，mock AI 即时响应，走完新用户全链路并计时；产出 0 项产品缺陷、15 项断言）：
  - **闭环链路（8.6s 自动化口径）**：定题（输入研究方向 → 生成建议 → 设为论文题目 → 主页徽标 12 字截断同步）→ 大纲（生成 → 采用 → 印章出现，大纲区+hero 各一枚）→ 写第一段（execCommand 用户路径插入正文 → 防抖落盘）→ AI 润色（选中文字 → 结果写入 + 「已应用」toast）→ 插入引用（文献 AI 解析入库 → 工作台下拉选择 → 正文 [1] 出现）→ 计划打卡（设截止 → 甘特图 → 打卡落库）→ 主页旅程 5/5 满态
  - **KR1 计时结论**：自动化口径 8.6s（mock 即时响应，不含 AI 等待）；真实 DeepSeek 每步约 10-30s，全链路预计 2-3 分钟，远低于 10 分钟红线——KR1 验收标准成立（自动化部分；真机计时留待作者自测）
  - **每步状态机反馈全部正确**：徽标同步、印章、落盘、toast、引用入库、甘特图、打卡、旅程满态——无卡点、无静默失败
  - 质量验证：新增 Round 21 套件 15 项断言（上述 12 个闭环节点 + KR1 计时 + 零 console 错误）；全量回归 **377 项断言二十一套件全过**
- **决策记录**：KR1 的验收口径定为「自动化闭环走查 + 真机计时补充」双层——自动化验证链路完整性与每步反馈（本套件），真机验证真实 AI 等待时间与打字手感；两处断言修正为测试问题非产品缺陷（徽标 12 字截断需检查前缀、印章可能两处各一枚）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第二十二轮：AI 输出容错走查（第 40 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（真实 DeepSeek 输出远不如测试 mock 规范——markdown 加粗、## 标题、列表符、编号变体、尾随逗号、前后说明文字是高频真实形态；此前三个解析器只吃「规范输出」，产出 3 项 P2 容错修复、13 项脏输出断言）：
  - **parseOutline 容错（P2，章节丢失风险）**：`**第一章 绪论**`、`## 第一章`、`- 第三章` 此前正则锚定「第」字开头全部不识别 → 大纲章节静默丢失、「大纲解析失败」toast；现先清洗 markdown 修饰（**、__、反引号、行首 #/-/•），编号变体补齐 `1、`/`1．`（此前只认 `1. `）；**清洗保留数字前缀交给 num 分支**（剥掉会让「1. 绪论」丢章，第一版正则误剥已修正）
  - **parseSuggestions 容错（P2，题目星号污染）**：`1. **基于注意力机制的医学图像分割研究**` → title 带 `**` 星号,设为题目后主页徽标/摘要生成全部带星号;现清洗 title 的 `**`/`__`/反引号
  - **citation.js 解析对齐 litsearch.parseJson 模式（P2，误拒收）**：前缀说明（「好的，以下是解析结果：」）、```json 围栏、**尾随逗号**、包装对象 `{"items":[…]}` 此前直接 JSON.parse 失败 → 拒收「AI 返回格式异常」（DeepSeek 高频形态）;现两段式容错（先直接解析 → 失败提取数组子串+去尾随逗号 → 解包装对象），与 litsearch 同构
  - **观察记录（不修）**：AI 输出「好的，以下是改写内容：」类前缀杂讯在四动作/摘要写入路径仍会进编辑器——逐字正则清洗脆弱（变体无穷），用户可编辑，留待后续
  - 质量验证：新增 Round 22 套件 13 项断言（脏选题清洗、5 章 markdown 包裹全识别+无残留+印章、脏 JSON 入库 2 条字段完整、干净链路不误伤、零 console 错误）;全量回归 **390 项断言二十二套件全过**
- **决策记录**：容错解析三原则——①快路径优先（先直接 parse，失败再容错，不牺牲干净输出的性能与确定性）；②清洗只去 markdown 修饰、保留结构化前缀（数字编号）；③与既有 parseJson（litsearch）模式同构，后续新解析点照此实现
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第二十三轮：可访问性审计（第 41 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（成熟产品底线——此前全站零 aria 语义、零键盘专项验证；产出 1 项 P2 修复 + 13 项断言）：
  - **toast 反馈无读屏语义（P2，KR6 对读屏用户失效）**：全站反馈中枢（成功/错误/引导 toast）无 aria-live，屏幕阅读器完全听不到任何操作反馈 → `#toast-wrap` 挂 `role="status"` + `aria-live="polite"`（容器已在初始 DOM，语义随反馈自动播报，一处覆盖全站）
  - **键盘可达性验证（0 缺陷）**：六视图核心控件 JS focus 可达；连续 12 次 Tab 焦点链不断（不丢到 body）；侧栏导航键盘 Enter 激活正常切模块；编辑器/生成按钮均可聚焦
  - **主题色 WCAG 对比度（0 缺陷）**：纸墨朱砂七组配色全部 ≥ 4.5:1（AA 普通文本标准）——cinnabar 白字 5.38、pine 白字 5.02、ink 白字 13.39、ink-blue 白字 8.64、paper 上 ink 12.27、paper 上 cinnabar 4.93、paper 上 pine 4.60（最小 4.60 达标）
  - 质量验证：新增 Round 23 套件 13 项断言（aria 语义、播报内容、焦点链、Enter 激活、五视图 focus 可达、对比度固化、零 console 错误）；全量回归 **403 项断言二十三套件全过**
- **决策记录**：可访问性按「反馈语义 + 键盘可达 + 对比度」三档验收，全部原生元素（button/input/textarea）天然可达，无需改 DOM 结构；对比度算法固化为断言防未来换色回归
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第二十四轮：KR2 五条任务链一次性走查（第 42 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（KR2「五条任务链走查一次性完成率 100%」此前无完整链路验收——各链被拆在 R14/R16/R21 等轮次验证，缺「空数据起点一次走完、无回退」的整体证据；本轮从**连 Key 都没有**的空起点一次走完五链；产出 0 项产品缺陷、19 项断言）：
  - **链 1 首次配置**：填 Key → 测试连接成功 → 保存 → 「回论文主页看下一步 →」引导出现 → 点击回主页
  - **链 2 选题采用**：输入方向 → 生成 2 条建议（结果需 ≥2 条才渲染「设为论文题目」按钮——此前 R21 只测单条 mock 没踩到）→ 设题 → 徽标同步 → 生成大纲 → 采用 → 印章「已采用 · 3 章」→「去写作工作台」衔接按钮 → 到达工作台
  - **链 3 按章写作**：光标定位章节正文区（R10 标题行保护路径）插入第一段 → 选中 → AI 润色写入 → 标记本章完成（进度落库 + 按钮隐藏）
  - **链 4 文献批量推荐**：基于题目+大纲批量推荐（CrossRef 3 条英文 + OpenAlex 1 条中文）→ 全选 → 入库 4 条 → 印章 + 「去写作工作台」引导 → 工作台下拉选择 → 正文插入 [1]——**首次有完整链套件（此前只验过批量推荐单点）**
  - **链 5 计划打卡**：设截止 → 甘特图 → 打卡落库 → 按钮变「今日已打卡」
  - **链尾**：主页旅程 5/5 满态（五链全部完成闭环）；全程零 console 错误
  - 质量验证：新增 Round 24 套件 19 项断言；全量回归 **422 项断言二十四套件全过**
- **决策记录**：KR2 验收成立——「一次性完成率 100%」= 每条链的每步状态机（按钮启用/印章/toast/落库）+ 链间衔接按钮（回主页/去写作/去大纲）全部按预期流转，无回退重试；两处断言修正为测试问题（选题 mock 需 ≥2 条、标记完成后按钮是 display:none 隐藏而非移除）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第二十五轮：验收交付物落地（第 43 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（GOAL 暂停条件「产出可验收版本时提醒作者」的最后一块拼图——24 轮修复与验证成果散落在 dev-log/GOAL/memory，作者自测缺少一份「打开就能勾」的验收文档；产出交付物 1 份、无产品改动）：
  - **docs/acceptance.md 落地**：①六条 KR 自动化验收状态表（含证据出处：R21 KR1 闭环 / R24 KR2 五链 / R17 移动端 / R19 性能 / R23 可访问性 / R22 输出容错 / R20 提示词）——作者打开即知「哪些已由 422 项断言兜底」；②真机验收勾选清单（五任务链 + 数据安全 + 移动端三项 headless 覆盖不了的重点：软键盘遮挡选区工具条、低端手机性能、窄屏长 toast）——自动化的项标注「✓ 自动」可跳过，把作者自测时间聚焦在真机特有项；③两项已知观察项显式记录（AI 改题目行同步题目、前缀杂讯不清洗——有意保留，注明触发条件与回滚建议）；④自动化回归运行方式
  - 质量验证：无产品改动，全量 24 套件 **422 项断言**状态不变
- **决策记录**：验收文档的三层结构——自动化证据表（可信可查）→ 真机勾选清单（作者可执行）→ 观察项（透明记录设计取舍）；「可验收版本」的定义 = 自动化六 KR 全过 + 作者真机清单勾选完成 + 观察项知情
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第二十六轮：边界输入审计（第 44 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（24 轮从没系统测过极端数据——成熟产品「不崩」的底线；产出 0 项产品缺陷、14 项断言）：
  - **超长题目（216 字）**：徽标 12 字截断不崩、项目完整入库、工作台首行完整渲染
  - **空值/纯空格**：选题方向纯空格有「请先填写」提示、大纲空题目有提示、打卡空备注正常（备注可选）
  - **超长文献标题（360 字）**：手动录入入库成功、无报错、列表无横向溢出
  - **边界日期**：已过截止（2020）主页有「已过」提示、远古日期（1900）页面不崩
  - **重复打卡**：按钮禁用 +「今日已打卡」
  - **HTML 注入**：`<img onerror>` 标题转义不执行（R11 修复回归确认）
  - 质量验证：新增 Round 26 套件 14 项断言；全量回归 **436 项断言二十五套件全过**
- **决策记录**：两处断言修正为测试设计问题（超长题目需由 mock 建议返回而非输入方向——输入框是研究方向语义；徽标截断断言应验省略号而非截断点外的文字）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第二十七轮：错误文案统一审校（第 45 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（全站 50 条 toast/Error 文案逐条审校——错误归因准确是成熟产品底线，用户失败后需要知道「发生了什么 + 能做什么」；产出 1 项产品缺陷修复、12 项断言）：
  - **批量检索失败归因误导（P2）**：buildQueries 抛「格式异常」（AI 输出问题）与网络/Key 错误共用同一 toast「已退回原始关键词检索（可到「设置」检查 API Key）」——AI 输出格式问题时引导用户去查 Key，归因错误让用户白费功夫；且 catch 丢弃了 R13 已做好的可读错误文案 → 改为透传 `e.message`（网络/bad_response/格式异常各有准确归因），「AI 检索策略返回格式异常，已自动退回原始关键词」分支文案明确说明发生了什么与后果
  - 「请先…」句式全站统一确认（请先填写/请先输入/请先在…选择/请先生成/请先勾选/请先设置/请先粘贴/请先选择/请先在编辑器中选中要扩写的文字）；needSelection 仅由扩写动作触发、文案准确；litsearch「格式异常」冒泡路径确认被 runBatch catch 优雅降级（降级后仍出检索结果，非缺陷）
  - 无上下文时批量推荐按钮条件隐藏（title 与 outline 都为空时不渲染「批量推荐」入口，runBatch 内守卫 toast 为 race 场景双保险）
  - 质量验证：新增 Round 27 套件 12 项断言（降级归因准确 / 降级后仍出结果 / 不误导查 Key / 五处「请先…」句式 / 按钮条件隐藏 / 双 context 零 console 错误）；全量回归 **448 项断言二十六套件全过**
- **决策记录**：错误文案归因三原则——不透传技术堆栈（R13 已做）、不误导操作（本轮修复）、可操作（失败后告诉用户发生了什么 + 下一步能做什么）；条件渲染入口保留双保险守卫
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第二十八轮：文献库引用状态自查（第 46 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（文献库数据量增大后的可用性缺口——50+ 条裸列表无法自查「哪些文献真正被正文引用了」；GB/T 7714 规范要求参考文献应为正文实际引用项，论文提交前自查是真实工作流痛点；产出 1 项功能增强、14 项断言）：
  - **引用状态标注**：`collectCitedNums()` 汇总所有草稿正文中的 `[n]` 引用编号集合 → 每条文献加状态 chip（已引用=松烟绿 / 未引用=朱砂色）+ 卡片统计行「已引用 X / N 条 · 未引用 K 条（GB/T 7714 参考文献应为正文实际引用项，建议补引或删除）」
  - **全部引用确认态**：所有文献均被引用时统计行转松烟绿「全部已被正文引用 ✓」
  - **即时刷新**：手动录入/删除文献后 refreshLibrary 重算统计与 chip；草稿引用变化后重挂载即更新（懒同步模型，与全局一致）
  - 空文献库不渲染统计行；chip 与统计行在 390px 无溢出（R17 回归确认）
  - 质量验证：新增 Round 28 套件 14 项断言（状态 chip/统计行/即时刷新/重挂载刷新/全引用确认态/空态/零 console 错误）；全量回归 **462 项断言二十七套件全过**
- **决策记录**：自查信息用「建议」而非强校验——文献可有意保存待引（选题阶段收集的候选），产品只提示不阻断；统计行文案含规范出处（GB/T 7714）帮助用户理解为什么需要关注未引用项
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第二十九轮：学位目标字数提示（第 47 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（论文写作最基础的硬性要求——学位字数下限从未被产品提示；用户埋头写到交稿才发现差几万字是高频事故；产出 1 项功能增强、9 项断言）：
  - **目标字数提示**：`TARGET_RANGE` 常量（本科 1.5-3 万 / 硕士 3-5 万 / 博士 8-15 万 / 课程论文 5 千-1 万）+ `totalWords()`（各章草稿合计口径，与目录 chip 一致）→ 工作台状态行「硕士论文常见 3-5 万字 · 已写 1,500 字，距下限还差 28,500 字」；未达下限朱砂提示，达下限转松烟绿「已达下限 ✓」；未知学位/无大纲不显示
  - **实时更新**：输入防抖保存落盘后刷新（与保存链一致）；初始渲染即填充（模板 #wb-target 初始为空 → render 内 updateMeta() 首调）
  - 质量验证：新增 Round 29 套件 9 项断言（文案/样式/实时更新/已达下限/未知学位/无大纲/零 console 错误）；全量回归 **471 项断言二十八套件全过**
- **决策记录**：目标区间为「常见要求」非硬校验（不同学校差异大），文案注明仅供参考；**测试方法论教训：headless Chromium 的 localStorage 磁盘同步是异步的——seed 写入后立即 reload 会从磁盘旧快照恢复旧值（900ms 内实测复现），套件应改用「模块重挂载」（nav 离开再回来，应用实时读 localStorage）替代 reload，更快更稳**
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第三十轮：撤销/重做完整走查（第 48 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（用户早期反馈「输入框撤销不生效」——已修但只覆盖了引用插入单点，AI 动作/多步/重做/落盘一致性从未系统验证；产出 0 项产品缺陷、10 项断言）：
  - **AI 润色可撤销**：applyEdit（setSelectionRange + execCommand('insertText')）进入浏览器原生撤销栈——润色后撤销恢复原文 ✓、重做恢复润色 ✓
  - **撤销粒度**：撤销后输入新内容，再撤销只回退新输入（润色保留）✓
  - **多步回退**：再次润色后单次撤销恢复最初原文 ✓
  - **落盘一致性**：撤销链后防抖保存的草稿与编辑器一致（不残留 AI 文本）✓
  - 零 console 错误 ✓
- **决策记录**：两次测试方法论修正（均非产品缺陷）——①**headless Chromium 的键盘模拟不触发 textarea 原生撤销**（keyboard.press('Control+z') 无效，即使焦点正确），套件改用 `ta.focus() + execCommand('undo'/'redo')` 等价真实用户快捷键；②线性撤销栈语义：撤销后再撤销会「重做之前被撤销的操作」（Word/Google Docs 标准行为），多步回退断言应验「一次撤销」而非盲目 ×2
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第三十一轮：插入引用体验增强（第 49 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（写作工作台插入引用是文献链与写作链的关键衔接点——原生下拉在文献 50+ 条时无法查找目标文献，只能靠滚动硬找；产出 1 项功能增强、13 项断言）：
  - **未引用文献排序优先**：下拉按「未引用（litNo 升序）→ 已引用（litNo 升序）」排列——用户大概率引用尚未引用的文献，减少重复引用；option 文案补充「· 未引用」标记，与文献库朱砂 chip 语义一致
  - **编号直达输入**：下拉旁新增 `#w-cit-no` 数字输入框——知道编号（如刚从文献库看到 [12]）时直接输编号点按钮即插，绕过下拉查找；输入有效编号优先于下拉当前值；Enter 键也可触发插入
  - **无效编号拦截**：文献库中不存在的编号（如 99）→ 朱砂 toast「文献库中没有编号 [99]」且不插入，避免产生悬空引用
  - **插入后即清空**直达输入框，避免误重复插入
  - 质量验证：新增 Round 31 套件 13 项断言（排序/标记/直达插入/计数重渲染/无效拦截/select 回退/Enter 触发/零 console 错误）；全量回归 **494 项断言三十套件全过**
- **决策记录**：未做自由文本搜索下拉（自定义 combobox 状态复杂度高、原生 select 无障碍免费）——「未引用优先排序 + 编号直达」覆盖绝大多数真实场景（用户或刚入库知道编号，或引用还没引用的文献），键盘可达性零回归
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第三十二轮：全文下载为 Markdown（第 50 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（论文最终要提交 Word/PDF——「复制全文」只进剪贴板，没有落成文件的出口；产出 1 项功能增强、13 项断言）：
  - **下载全文按钮**：工作台工具栏新增 `#wb-download`——复用 fullText()（题目+摘要+关键词+各章+自动参考文献+致谢），Blob + a.download 下载为 Markdown 文件
  - **文件名清理**：论文题目中的 `\ / : * ? " < > |` 替换为下划线（跨平台文件名合法），空题目兜底「论文全文.md」
  - **缺号提示复用**：正文引用了文献库不存在的编号时，下载与复制/预览同款朱砂 toast 提示
  - **下载即落盘**：persistDocument 先保存再下载，文件与本地一致；下载成功 toast 含字数
  - 质量验证：新增 Round 32 套件 13 项断言（文件名清理/内容完整性/GB/T 7714 只列正文实际引用项/缺号提示/空题目兜底/下载前落盘/零 console 错误）；全量回归 507 项断言三十一套件全过
- **决策记录**：导出 Markdown 而非 Word——纯静态无重依赖（docx 生成库不进浏览器），MD 可被任意工具转 Word/PDF；未引用文献不进入参考文献列表（GB/T 7714 语义，与 R28 自查口径一致）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第三十三轮：文献库实时搜索（第 51 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（文献 50+ 条后库内找文献靠滚动——与 R31 插入引用同源缺口的另一半；产出 1 项功能增强、17 项断言）：
  - **搜索框**：文献库列表上方 `#cit-search`（type=search）——按 标题/作者/出处/完整格式 任一包含关键词匹配（不区分大小写）
  - **实时过滤**：input 即过滤（局部刷新 refreshLibrary），搜索词在局部刷新中保留（入库新条目不丢搜索）；匹配计数「匹配 X / N 条」，零匹配转朱砂 + 空态文案「没有匹配「xxx」的文献」
  - **清空按钮**：✕ 一键清空并聚焦回搜索框；空库时禁用
  - 质量验证：新增 Round 33 套件 17 项断言（四字段匹配/计数/空态/清空恢复/搜索词保留/空库禁用/零 console 错误）
- **决策记录**：搜索字段含 formatted 完整格式（用户常复制粘贴完整引用搜索）；不做下拉联想（列表就在下方，过滤即所见）
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第三十四轮：触屏撤销/重做按钮（第 52 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（R30 验证了 Ctrl+Z/Y 可撤销 AI 编辑——但 iPad/触屏笔记本没有快捷键，触屏用户无法撤销 AI 动作；产出 1 项功能增强、10 项断言）：
  - **工具栏撤销/重做按钮**：工作台工具栏「↶ 撤销」「↷ 重做」（ghost 按钮）——点击 = ta.focus() + execCommand('undo'/'redo')，与 Ctrl+Z/Y 走同一个原生撤销栈（R30 验证路径），AI 编辑与手输混合可回退
  - **反馈**：撤销/重做成功 toast（1.5s 短提示）；按钮 title 注明键盘等效（Ctrl+Z / Ctrl+Y）
  - **390px 验证**：工具栏新增按钮后无横向溢出、按钮在视口内可见
  - 质量验证：新增 Round 34 套件 10 项断言（按钮存在/润色可撤销/可重做/toast/落盘一致/390px 布局/零 console 错误）
- **决策记录**：不做禁用态（execCommand 无可靠的状态查询 API，点击无操作时无害且 toast 已给反馈）；触屏场景是 R30 的补位而非替代
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-19 · 体验优化专项第三十五轮：文献检索「加载更多」分页（第 53 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（单次检索默认只取 10 条候选——真实文献检索一次 10 条不够选；产出 1 项功能增强（含修复 1 个实现期真 bug）、14 项断言）：
  - **分页加载**：searchLiterature/searchCrossRef/searchOpenAlex 增加 offset 参数（CrossRef/OpenAlex 原生支持）；单次检索后列表底部「加载更多（同一检索词）」按钮——追加下一页、按 DOI/题名去重、总数计数更新、无更多时按钮移除并提示
  - **勾选保留**：追加渲染不重绘已有点（insertAdjacentHTML 追加到 .item-list），首屏默认勾选「推荐前 3」只生效一次
  - **事件委托**：勾选计数与详情展开改为 results 容器委托——追加条目无需单独绑定；lit-add 改引用 currentItems（追加条目同样可入库）
  - **批量推荐不分页**：runBatch 置空 moreState（批量链路保持原样）
  - **修复实现期 bug**：追加批次下标从 0 重算导致 id 冲突（lit-cb-0 重复、详情按钮指向错误条目）——litItemsHtml 增加 startIndex 全局下标续接
  - 质量验证：新增 Round 35 套件 14 项断言（首批 10 条/追加去重/总数更新/勾选保留/追加条目详情与入库/无更多收尾/批量无分页/零 console 错误）
- **决策记录**：offset 分页而非 cursor（OpenAlex 推荐 cursor 但 CrossRef 只有 offset，统一 offset 简单可靠）；每页 10 条保持与既有检索行数一致
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-20 · 体验优化专项第三十六轮：素材一键插入正文（第 54 次会话）

- **人的工作**：（作者未介入，自主推进轮次）
- **AI 的工作**（素材库此前只有复制/删除——保存素材的意义是复用，缺了最直接的「插进正文」交互；产出 1 项功能增强、10 项断言）：
  - **插入按钮**：素材条目新增「插入」——点击在编辑器光标处 applyEdit 插入素材全文（与引用插入同款机制，进原生撤销栈），toast「已插入素材「标题」（Ctrl+Z 可撤销）」
  - **多行保留**：素材内容（如摘要含空行分段）原样插入，不被 HTML 解析改动
  - **可撤销**：R34 触屏撤销按钮 / Ctrl+Z 均可回退；插入后防抖落盘与编辑器一致
  - **空态**：空素材库不渲染插入按钮
  - 质量验证：新增 Round 36 套件 10 项断言（按钮存在/光标处插入/多行保留/toast/撤销恢复/落盘一致/空态/390px 无溢出/零 console 错误）
- **决策记录**：不做插入确认弹窗（可撤销即安全网，弹窗打断心流）；标题截断 12 字进 toast 防溢出
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 产品评审 P0 批次落地（第 55 次会话）

- **人的工作**：
  - 对三视角评审的 5 个决策问题给出拍板：**保留无后端/本地数据**；商业化主路径看建议 → 近期以 C（学校/教师 B2B2C）为主、B（托管额度）待放开红线；**人群不局限、兼容全体但补在职增强**；**AI diff 预览列入下一轮**；演示/试用看建议。
  - 批准第一轮 P0 整批开工：① AI diff＋② 研究设计回填＋③ 写作台状态一致＋④ 计划页去冗余。
- **AI 的工作**（落地 4 项 P0，逐项验证，零 console 错误）：
  - **① AI 前后 diff 预览**：`writing.js` 增加 `diffTokens / diffTokensFrom / inlineDiffHtml`（西文词与数字归组、单个汉字一 token 的轻量 LCS 差分），右侧建议框在「学术润色/补充论证/重构表达」三类重写动作下显示**前后对比**（`<s>删除线</s>`=原文去掉、`<b>高亮</b>`=新增），保留「建议全文」与接受/拒绝/重新生成；续写/逻辑检查不误用 rewrite diff。
  - **② 研究设计回到项目预填**：`topic.js` 新增 `researchDesign.stepTouched` 区分「首次进入/手动切步」；`currentStep()` 在项目已推进（已有题目/大纲）且用户未手动切步时默认落在**已达成阶段**（不再从 step1 空表单开始）；step1 增加「你已定题：… 可细化或直接下一步」提示条；手动切回 step1 尊重用户选择（stepTouched）。
  - **③ 写作台「正在写/当前章节卡」一致**：编辑器初始化后 `alignInitialChapter()` 把光标落在「正在写」章节，三处（顶栏/目录反色/右侧卡）一致；并修复光标落在摘要/关键词等**非章节区域**时右侧卡错误回退到第一章的残余问题（改为回退到 currentChapter 对应章节）。
  - **④ 计划页任务卡去冗余**：`planner.js` 每卡由「去处理 + 标记完成」双按钮 → **完成勾选（`.task-check`，松烟绿）+ 单一主行动「去处理/已完成」**。
  - `css/style.css` 增加 `.pm-diff-del / .pm-diff-ins / .task-check / .topic-already`。
  - 质量验证：Playwright 探针逐项断言——①diff 渲染（del/ins 计数>0）②主题默认落 step3、切回 step1 出「已定题」条 ③顶栏==右卡（第二章）④任务卡 11 主行动 + 11 勾选、0 冗余按钮；390px 计划/写作页零横向溢出；全程零 console 错误。
- **决策记录**：AI diff 只对三类「重写」动作做差异展示，续写/逻辑检查（新增/批注语义）保持原文+建议双框，避免误导；章节一致性以「正在写」章节为基准（顶栏/卡/目录对齐），非章节区光标时卡回退到当前章节而非第一章。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 演示/试用入口落地（第 56 次会话）

- **人的工作**：作者拍板「无后端/本地数据」，商业化近期以 C（学校/教师 B2B2C）为主；点名下一轮推进。
- **AI 的工作**（回应评审 Q5「默认演示/试用入口」；产出 1 项功能、零 console 错误）：
  - **空态「开始试用 · 无需配置」**：项目中心空态新增按钮（`js/modules/projects.js`），点击 = `hasExistingData()` 确认覆盖 → `loadDemoData()` → 进入论文主页；并附一句说明「演示项目走完整流程，AI 为内置示例不消耗额度；真实 AI 到设置填 Key」。
  - **全局「演示模式」口径**：`js/app.js` 顶栏 API 徽章重做三态——已配 Key 且启用真实 AI →「API 已配置 · model」（松烟绿）；已配 Key 但未启用 →「演示模式 · 已配 Key 未启用」（金）；无 Key →「演示模式 · Mock 结果」（金，带 title 说明）。`css/style.css` 增加 `.status-pill.demo` 与 `.empty-actions`。
  - 与现有 `enableLiveAI`（设置页「启用真实 AI 调用」开关）语义对齐：Live=真实模型，否则=演示/模拟。
  - 质量验证：Playwright 探针——空态存在试用按钮、无 Key 顶栏转「演示模式 · Mock 结果」、点击后进入论文主页并载入演示项目、无 console 错误。
- **决策记录**：「开始试用」属用户动作，用 ghost（非朱砂）样式，遵守「朱砂=AI 动作」的颜色语义；默认 Mock 与「演示模式」统一口径，避免用户误把示例当真实 AI。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 体验优化方向确立 + 移动端溢出修复 + 计划定稿（第 57 次会话）

- **人的工作**：作者明确方向——**不再做大的新功能**，聚焦「现有功能流程完整性 / 用户交互体验 / 页面 UI」；要求基于此方向定计划。
- **AI 的工作**：
  - 全模块走查（Playwright，桌面 1440 + 移动 390，六模块+设置）：桌面零报错零溢出；**移动端论文主页横向溢出 324px**。
  - **修复移动端论文主页溢出**：根因 `.grid-2` 用 `1fr` 轨道被内容 min-content 撑开（写作之旅六步横条 + 今日焦点卡）；改为 `minmax(0,1fr)` + `.grid-2>*{min-width:0}` 通用防溢出（沿用 GOAL R10 惯例）。**修复后六模块+设置移动端溢出全部归零**。
  - 输出 `docs/experience-polish-plan.md`：按「A 流程完整性 / B 交互体验 / C 页面 UI」三块给出具体可验收清单 + 量化验收标准 + 建议轮次节奏 + 4 项待确认问题。
- **决策记录**：本轮不引入新功能；「开始试用」按钮语义色由朱砂改回墨蓝（朱砂=AI 动作），守住设计语言。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 体验优化 R2：写作台交互/护栏 + AI 前缀清洗（第 58 次会话）

- **人的工作**：确认轮次顺序（A1 流程完整性 → B 交互 → C 观感）；移动端真机项暂不考虑；同意写作台右侧卡片按钮分组收纳（兼顾交互与美观）；同意把「AI 动作改题目行」与「AI 输出前缀杂讯」一并优化。
- **AI 的工作**（落实 3 项，全模块回归零报错零溢出）：
  - **写作台右侧卡片按钮分组收纳**：原 6 个操作按钮一格排 → 按「上一/下一章」+「结构调整（上移/下移）」+「编辑本章（改标题/前插/后加/删除）」分组，加 `.wb-side-label`（小标题+分隔线）压降视觉噪音；`[data-section-action]` 跨两组仍复用同一绑定，交互不变。
  - **AI 动作改题目行护栏**：新增 `selectionHitsHeading`——当选中范围命中题名/章节标题时，AI 改写动作直接拦截并提示「标题会同步回写项目结构，请不要对标题做 AI 改写」，避免标题被 AI 静默改写并同步污染项目主信息。
  - **AI 输出前缀杂讯清洗**：`ui.js` 新增 `cleanAiText`，写入正文前剥掉「好的，以下是改写内容：」等常见口语/元前缀（保守清理，不伤正常正文，如「研究问题：…」保留）；应用于写作 AI 建议与章节草稿两处输出。
  - 质量验证：分组标签渲染（结构调整/编辑本章 + 6 个动作仍可用）；标题护栏 toast 触发且不执行 AI；`cleanAiText` 单测通过常见前缀/正常文本；全模块桌面+移动 0 溢出、0 console 错误。
- **决策记录**：AI 前缀清洗采用保守策略（只剥开头客套/元前缀，不去碰正文中的冒号标签），与评审「逐字清洗脆弱、变体无穷」的判断一致，够用且不伤内容。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 体验优化 R3–R6 收尾（第 59 次会话）

- **人的工作**：要求自主安排计划、把这次总任务（体验优化计划 R3–R6）做完再统一审核，不用改一点就停。
- **AI 的工作**：
  - **R3（流程完整性走查）**：全模块 + 数据一致性 + 导出链路走查——改题目后徽标/hero/写作首行三处即时同步；MD 导出有内容、排版预览窗口正常开；计划任务/打卡正常；零报错。**结论：六条主流程无断链、无需改动**（与 R24 五链零缺陷一致）。
  - **R5（页面 UI）**：论文纸面层级区分——题名居中、摘要/关键词居中弱化、章节主体突出、参考文献/致谢用分隔线收尾（通配 `[data-role]` 样式，安全无交互影响）；此前已完成右侧卡片分组收纳与移动端溢出修复。
  - **R4（交互体验）**：确认 AI 动作按钮 + 选中处理 + 标题护栏 + 反馈闭环均正常；未引入新功能。
  - **R6**：最终全量走查（桌面 1440 + 移动 390）六模块+设置 0 溢出、0 控制台错误；更新 `docs/acceptance.md`（体验优化进展 + 两项观察项已处理）。
- **决策记录**：用户已排除移动端真机项（软键盘/低端机），本轮不做；「选区快捷操作条」沿用现有顶栏 AI 动作按钮交互（不新增功能）。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 专项 UI/UX 审计 + P1 修复（第 60 次会话）

- **人的工作**：要求做一轮系统的、专项的 UI/UX 审计。
- **AI 的工作**：
  - 产出 `docs/ui-ux-audit.md`：①设计令牌一致性盘点（116 处硬编码色且大量与令牌近似；22 个字号值含 0.5px 近重复；圆角/阴影散落）②逐屏走查（六模块+设置+写作）③组件状态矩阵（空/载/错/完成/禁用覆盖较好）④Nielsen 10 启发式评估（最低：一致性3/美观3/帮助文档2）⑤分级问题清单（0 个 P0 / 3 个 P1 / 6 个 P2）。
  - **修复 P1**：①语义色收敛——写作工作台 AI 按钮由硬编码 `#C84533`（局部不一致）改为全局 `--cinnabar`/`--cinnabar-soft`/`--cinnabar-rail`/`--cinnabar-strong`；占位与 AI 删除线灰色收敛到新增 `--mute`；②字号阶梯收敛——11.5/12.5/13.5/14.5/17.5px 向上取整为 12/13/14/15/18px，字号全部整数化。
  - 质量验证：写作台 AI 主按钮解析为 `rgb(192,59,45)`（--cinnabar）；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：P1 第 3 项（写作台顶部动作密度）判为 P2 类（改头部布局更侵入、收益边际递减，本轮不动，已在审计报告标记）；P2 各项（设计令牌系统化、留白统一、帮助入口等）列入后续。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 真实用户走查 + 修复真实交互/视觉问题（第 61 次会话）

- **人的工作**：反馈"上次审计太细、用户注意不到；关注真实用户使用时的交互问题与明显视觉错误"。要求以真实用户视角再走查。
- **AI 的工作**（改用真实用户角色走查，修正审计重心）：
  - 走查发现并修复 2 个真实痛点：
    - **① 新建项目 → 写作 = 空脚手架**：新项目无大纲时，右卡只说"先选中一个章节再开始写"、把加章按钮藏起，左目录提示"在编辑器新增章节标题"却没有入口。→ 空态右卡加「新增第一章 / 去研究设计」按钮；新增 `addFirstSection()`（插到参考文献标题前，并同步当前章节/目录/右卡/顶栏）。
    - **② 计划任务卡完成勾选是空白圆**（✓ 透明，像没做好的元素）→ 改为未完成显示浅灰 ✓、悬停变绿、完成实心绿。
  - 走查确认无问题：改题目三处同步、导出 MD/预览、计划/打卡、设置页清晰。
  - 记录（未处理、待定）：右侧"插入引用"等藏在"右侧工具 ▾"折叠里（可发现性）；创建项目落在项目设置（可改直接进写作/研究设计）。
  - 更新 `docs/ui-ux-audit.md`：新增「真实用户走查发现」为最高价值阈值，设计令牌/字号等降为 P2。
  - 质量验证：空写作新增章后 header/目录/右卡一致；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：审计重心从"设计细节"转向"真实用户走查"；「新增第一章/去研究设计」为临时性空态引导，未来仍鼓励走研究设计出大纲。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 右侧工具标签 + 创建项目落点（第 62 次会话）

- **人的工作**：同意处理两项走查待定——①右侧工具可发现性 ②创建项目后落点。
- **AI 的工作**：
  - **① 右侧工具改为常驻可见标签栏**：写作台右侧从「右侧工具 写作助手 ▾」折叠 `<details>` 改为**六枚常驻标签**（写作助手/引用/证据/模板/待修改/版本），激活标签高亮、悬停有 tooltip 说明；`switchRightTab` 改为对 `[data-side-tab]` 切换 active + aria-selected；清理死掉的 `.wb-side-switcher` 相关样式，新增 `.wb-side-tabs/.wb-side-tab`（含 980px 收起）。插入引用等高频工具**不再需要先展开**。
  - **② 创建项目后落点**：由「project-settings」改为**论文主页**（产品首页，含写作之旅指引）；toast 改「论文项目已创建，先到论文主页开始写作」。
  - 质量验证：6 标签渲染、点「引用」正确切到 citation 面板；创建项目落在论文主页；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：右侧工具标签常驻（恢复 R23 之前的可见性）是基于"引用高频、需可发现"的现实判断；创建后落论文主页（首页引导）优于直接进设置（减少官僚感）。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 写作台顶部/右栏重设计（第 63 次会话）

- **人的工作**（给截图+3 点反馈）：①标题字多时工作台顶部按钮错位；②右侧「当前章节」整卡占太多空间、使用低频、点章节不自动跳、应让工作区内容上移把章节标题移到顶部；③工作区下方标签与功能 UI/交互丑、需重设计。
- **AI 的工作**：
  - **① 顶部按钮错位**：`.wb-current` 加 `flex:1 1 0;min-width:0;overflow:hidden`，`.cur-title` 加 nowrap+ellipsis，`.wb-header-actions` 改 `flex:0 0 auto;white-space:nowrap`——超长章名截断为「第4章 基于可…」，按钮稳定在一行右对齐，不再错位（已用超长章名截图验证）。
  - **② 当前章节卡瘦身 + 点章节滚到顶**：把大面积「当前章节」整卡（标题/状态/4 指标/上移下移/改标题/前插后加/删除）重做成**紧凑条**——标题(截断)+状态 chip + 字数/引用/待改/证据一行 + 上一章/下一章 + 「操作 ▾」下拉（上移/下移/改标题/前插/后加/删除，删除红色）。`jumpToSection` 改为把章节标题滚到视口顶部（rAF+`nodeDOM` 对齐，用 `document.scrollingElement.scrollBy`）；点目录/上一章/下一章均生效。
  - **③ 右栏标签重设计**：去掉原来的灰渐变盒，改为干净标签栏（active=品牌浅底+品牌描边）；`.wb-side-tab` 激活态用 `--brand-soft/--brand-strong`。
  - 新增 `.wb-ch-*`（紧凑条）与清理死掉的 `.wb-side-*` 章节卡样式（保留面板容器类）。
  - 质量验证：紧凑条 + 操作下拉可开合；标签激活态渲染；切换到「引用」正常；点「下一章」跳到第三章并滚到视口附近；超长章名顶部按钮一行对齐；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：右栏重设计方向="紧凑信息+动作收纳到操作菜单+工具标签常驻顶部"；跳转滚动以「章节标题靠近视口顶部」为准（短文档因内容不足只会滚到接近顶部，属预期）。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 编辑区内部滚动 + 右栏重排（第 64 次会话）

- **人的工作**（2 点）：①跳转逻辑错了——不应滚整个页面，应只动文字工作区内容；②右边章节顺序调整（上移/下移）频率很低，没必要直接展开在右栏，应收进某个入口，把下面工具上提。
- **AI 的工作**：
  - **① 编辑区内部滚动**：`.paper-sheet` 改为内部滚动容器（`height: calc(100vh - 250px); overflow-y:auto;`，去掉 resize），`jumpToSection` 改为滚 `.paper-sheet.scrollTop`——点目录/上一章/下一章只滚动**词纸工作区**，页面不再整体滚动（验证：编辑区 scrollTop 805、页面 scrollY 保持 0）。
  - **② 右栏重排**：把工具标签栏**上提到右栏最上**（提高工具使用可达性）；当前章节压缩为**紧凑条**（状态 chip + 章节名截断 + ⋯ + 字数/引用/待改/证据一行 + 上一章/下一章）；**结构操作（上移/下移/改标题/前插/后加/删除）收进"⋯"菜单**，默认收起（修复 `[hidden]` 被 `display:grid` 覆盖导致默认展开的 bug，补 `.wb-ch-ops[hidden]{display:none}`）。
  - 质量验证：编辑区内部滚动（overflowY auto、scrollTop 变化、页面不动）；标签栏为右栏首个元素；结构操作默认收起、点 ⋯ 开合正常；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：编辑器为固定高度内部滚动容器（写作应用惯例）；结构操作收纳到 ⋯（低频、需时再展开）；工具标签常驻顶部（高频可达）。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 右栏顺序修正 + 标签栏重设计（第 65 次会话）

- **人的工作**：①顺序错了——标签要和它的工具内容在一起，当前章节放最上；②标签栏这块的功能夹在中间用起来很不合理、很不顺，让 AI 研究怎么设计。
- **AI 的工作**：
  - **顺序修正**：右栏改为「当前章节（顶部紧凑条）→ 工具标签 → 工具内容」，标签与内容强关联（不再夹在中间）。
  - **标签栏重设计为下划线式**：活动标签用品牌色下划线、内容紧跟其下，作为内容头部（去掉原来的胶囊盒）。同时**把 6 个标签收敛为 4 个核心写作工具**（写作助手/引用/证据/模板，一行放下更干净），把**待修改/版本**这两个"章节/文档管理"项挪到当前章节的「⋯」菜单里（`data-go-pane` 切换到对应面板）。
  - 质量验证：标签 4 个一行、活动标签下划线；「⋯」打开后点「版本历史」切到版本面板；右栏顺序=章节→标签→内容；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：右栏语义分组——**写作工具**（写作助手/引用/证据/模板）作标签常驻顶部，**章节管理**（待修改/版本/结构调整）收纳进当前章节「⋯」；标签用下划线式与内容连成整体。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 删除"章节模板"功能（第 66 次会话）

- **人的工作**：质疑模板功能意义——内容太多、还很挤，若不重要就删掉。
- **AI 的工作**：
  - 评估：模板是"填空式句式库"（研究背景段/方法说明/过渡句/结论收束等，带 `____` 占位）。属**低价值甚至有害**——固定句式库易诱导套话，且把右栏塞满。采纳作者判断**删除**。
  - 删除范围：`js/modules/writing.js` 移除——`SIDE_PANELS.templates`、`章节模板` 面板 HTML、`templateGroupsForSection`/`renderTemplatesPanel`/`insertTemplateText` 函数、`templatesBox`、三处 `renderTemplatesPanel()` 调用；`css/style.css` 移除 `.wb-template-*` 样式块。
  - 结果：右栏工具标签由 4 个（写作助手/引用/证据/模板）减为 **3 个（写作助手/引用/证据）**，更清爽；其余功能不变。
  - 质量验证：模板标签与面板不再出现；3 标签一行渲染；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：移除"填空式模板库"——它鼓励套话且价值低；保留真正服务于写作的工具（写作助手/引用/证据）。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 真实用户走查：修复"引用不可见"P0 bug（第 67 次会话）

- **人的工作**：转回真实用户走查。
- **AI 的工作**（走查发现并修复一个 P0 bug）：
  - **走查**：完整走核心旅程（首页→研究设计→写作→引用→文献→计划→导出→设置），并实测写作 AI 动作/插入引用/打卡等交互。
  - **发现 P0（引用不可见）**：插入引用后正文出现的是**空 `<span>`**——`citationViewFactory` 只在 `update` 里填充 class/文本，而 ProseMirror 在 nodeView **创建时**并不必然调用 update；且 `getCitationNumber` 读取 `viewState.view.state`，在编辑器**首次渲染时 view 尚未赋值（null）**导致抛错 → 引号节点渲染成空白。即正文里的 `[n]` 一直是**不可见**的（只有导出/复制时靠文档模型重算，用户看不到）。
  - **修复**：①`getCitationNumber` 改为 `viewState.view ? viewState.view.state.doc : editorState.doc`（首次渲染用初始 doc 兜底，能算对编号）；②`citationViewFactory` 改用闭包 `dom` 并在**创建时立即 sync**（照 footnote 可行模式），`update` 复用 sync。
  - 结果：demo 第二章引用正确显示 `[1]`/`[2]`；新插入的引用也可见编号；全模块桌面+移动 0 溢出、0 控制台错误。
  - **已知局限（记录）**：内联 `[n]` 编号在**插入更早的引用后不自动重排**（nodeView 只对内容变化的节点重渲染，位置变化不触发）；**导出/复制时的参考文献列表会按首次出现顺序正确重排**。此项为 P2，可后续处理。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 引用编号重排 + 方案摘要提示 + 主页卡片优化（第 68 次会话）

- **人的工作**：确认把走查发现的三项都处理（内联引用编号重排 / 研究设计方案摘要"未选择" / 主页"今天该做什么"冗余）。
- **AI 的工作**：
  - **① 内联引用编号重排**：新增 `refreshCitationNumbers()`——每次文档事务（dispatchTransaction）后按 `buildCitationNumberMap`（首次出现顺序）重新渲染所有 `.pm-citation` 的编号；插入/删除引用后内联 `[n]` 即时更新（此前只对内容变化的节点重绘、位置变化不触发，故新插入更早的引用后旧编号不更新）。验证：在摘要前插入 U-Net（未引用文献）后，第二章编号由 [1][2] 正确变为 [2][3]。
  - **② 研究设计方案摘要提示**：`renderPlanSummary` 在方案缺失（有 outline 但研究问题/方法/数据来源未单独设定）时，显示琥珀提示条"…尚未单独设定…可到「定方案」步完善" + 「去定方案」按钮（`data-go-step="2"`）；字段占位由"未选择"改为"未单独设定"。
  - **③ 主页"今天该做什么"卡片**：`.focus-check` 由带边框的盒（易被当成按钮、与 CTA"去配置"撞）改为安静的列表项（圆点+文字），弱化信息步骤、突出单一 CTA。
  - 质量验证：方案摘要提示+未单独设定；focus-check 无边框；引用重排 [1][2]→[2][3]；全模块桌面+移动 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 研究设计模块 UI/交互整体提升（第 69 次会话）

- **人的工作**：指出研究设计模块仍有很多 UI 不合理（附"如果这一批都不满意"反馈区 + 学术诚信提示条示例），交互流程不顺畅，要求整体提升一轮。
- **AI 的工作**（走查第2/3步后统一重设计）：
  - **候选卡片更干净**（`.topic-option-card`）：选中用品牌色边+品牌浅底+hover，文字加深；去掉原本的重边框/密盒感。
  - **属性 chips 弱化为元数据**（`.topic-meta-pill`）：去边框、更小、更淡（`--mute`），不再像可点击标签与题目抢视觉。
  - **反馈区更轻**（`.topic-feedback-block`/`box`）：更紧凑、占位更少。
  - **学术诚信提示更轻**（`.integrity-note`）：字号更小、左侧边线变浅（全局一致）。
  - **第3步大纲重复消除**：结构化大纲预览（`#outline-out`）收进可折叠 `<details>`（"▸ 预览大纲结构"，默认收起），可编辑 textarea 作为主编辑区（"编辑大纲（可直接改章节名、增删二级标题）"），并下调 textarea 高度——不再一份大纲展示两次、页面大幅变短。
  - 质量验证：第2步候选卡/元数据/反馈、第3步大纲折叠+编辑区均正常；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：研究设计统一为"轻卡片 + 弱化元数据 + 单一可编辑大纲"；反馈/诚信提示降噪但保留合规含义。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 研究设计问卷精简：移除"都不满意"反馈框（第 70 次会话）

- **人的工作**：指出问卷底部"如果这一批都不满意 + 结合意见重生成方案"一直挂在那（不管啥问题都在），与左上角"重新生成这一轮方案"重叠；既然每步都有"或者直接写你自己的答案"自定义输入，这块基本可以不存在。问 AI 怎么看。
- **AI 的工作**（认同并精简）：
  - **移除** 第2步"如果这一批都不满意"反馈框（`feedbackBlock('rd-plan-feedback',...)`）及 `#rd-plan-feedback-submit` 处理器；第3步大纲反馈框（`outline-feedback`）一并移除（大纲可直接编辑）。
  - 保留一个**轻量"换一批方案"**按钮（原"重新生成这一轮方案"重命名），候选很差时一键重来，无需打长反馈。
  - 保留每步"或者直接写你自己的答案"自定义输入（覆盖具体偏好）。
  - 决策：用"每步自定义输入 + 单个换一批方案"替代"总挂着的反馈框"，更简洁且不丢能力；第1步题目反馈框保留（题目无自定义输入、该反馈是唯一的精化入口）。
  - 质量验证：反馈框消失、"换一批方案"按钮生效、自定义输入保留；全模块桌面+移动 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 研究设计再精简：删"换一批" + 第3步大纲页重排（第 71 次会话）

- **人的工作**：①"换一批方案"意义不大、占地方碍事，删掉；②第3步大纲页内容太多、排布乱、找不到重点，整体调整。
- **AI 的工作**：
  - **删掉第2步"换一批方案"按钮**（`rd-plan-retry` 从正常态移除；错误态的重试按钮独立保留）。配合此前的自定义输入，候选不满意就直接打字，不再保留单独的重生成入口。
  - **第3步大纲页重排，突出大纲焦点**：把"方案摘要（题目/研究问题/方法/数据 + 研究目标/空白/判断）"整体收进**可折叠的"▸ 已确定方案"**（默认收起，可展开查看）；"生成并微调大纲"（生成/复制/采用按钮 + "▸ 预览大纲结构" + 可直接编辑的大纲 textarea）作为**主体**；词句降噪（去掉"给一句修改意见让系统重生成"等已删功能的残留文案）。
  - 结果：第3步从"一长条堆叠"变为"大纲编辑为主体、方案摘要折叠"的清爽布局，页面大幅变短、重点明确。
  - 质量验证：第3步"已确定方案"折叠可展开、大纲编辑正常；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：研究设计最终交互="自定义输入为主 + 大纲直接编辑"；不再保留"反馈框/换一批"等重生成入口（避免与自定义输入重叠、减少 UI 噪音）；方案摘要降为可折叠的上下文。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 第3步大纲页两栏布局重排（第 72 次会话）

- **人的工作**：指出第3步 UI 仍丑——内容都靠左堆、右边全空，要求重新设计布局。
- **AI 的工作**：
  - 把第3步由"全宽单列靠左堆"改为**两栏**：左=`生成并微调大纲`（生成/复制/采用按钮 + ▸ 预览大纲结构 + 编辑大纲 textarea），右=`已确定方案`（折叠侧栏，可展开查看题目/问题/方法/数据+研究目标/空白/判断）+ 使用提示（"点生成→下方改章节→点采用"）。
  - 新增 `.topic-outline-layout`（grid，`minmax(0,1fr) 320px`，≤980px 变单列）、`.topic-outline-side`、`.topic-outline-tip`。
  - 效果：横向空间用起来、左右两栏平衡，大纲编辑仍是焦点，右侧提供方案上下文与操作引导。
  - 质量验证：两栏渲染、移动端单列回落；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：第3步采用"主任务(左) + 上下文/引导(右)"两栏结构，替代全宽单列靠左，避免右侧空置。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 第3步大纲页：回到上下布局 + 按钮进标题行右侧（第 73 次会话）

- **人的工作**：否掉上一轮两栏布局（"更难看了"），要求：整体保持**上下布局**即可；但**三个大按钮特丑、单占一行更丑**，应放到上面标题行的右边，并调整样式。
- **AI 的工作**：
  - **回退两栏** → 单列上下布局（移除 `.topic-outline-layout` 等两栏样式与结构）。
  - **三个操作按钮移到"生成并微调大纲"标题行右侧**（`.topic-candidate-head-actions` flex：标题/说明左，按钮右），不再单占一行、不再是大号红/深按钮；改为**紧凑小按钮**：生成大纲(ghost)/复制文本(ghost)/采用当前编辑稿(深色)。≤640px 按钮换行占满。
  - 方案摘要保持折叠的"▸ 已确定方案（题目/研究问题/方法/数据来源）"在上下流里。
  - 效果：上下布局、标题行右侧一组紧凑操作按钮，重点（大纲编辑）明确、也不再左堆右空。
  - 质量验证：按钮右对齐、上下布局；全模块桌面+移动 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 第3步聚焦"出大纲"+ 采用前确认（第 74 次会话）

- **人的工作**：指出这页关键应是"出大纲"，不该上来"确认方案"；如需确认应单独做，或在**点"采用大纲"时弹窗确认**再应用到写作台；`采用当前编辑稿`文案不对，直接"采用大纲"即可。
- **AI 的工作**：
  - 页头"确认方案，生成并采用大纲" → **"生成并采用论文大纲"**（desc 改为"大纲按已定方案生成，可直接修改，采用后同步到写作工作台"）。
  - 按钮"采用当前编辑稿/采用此大纲" → 统一 **"采用大纲"**。
  - **采用前加确认窗**：点击后 `confirm('确定采用这份大纲（N 章）吗？采用后将同步到写作工作台。')`，取消不采用；确认后 `adoptOutline` 并跳转写作工作台。
  - 方案确认已由"问卷（第2步）+ 采用前确认窗"承载，不再占用页面标题。
  - 质量验证：页头/按钮文案正确；点击弹确认（5 章）→ 确认后进入写作工作台；全模块桌面+移动 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 第3步：自动生成大纲 + 自定义确认弹窗 + 冲突处理（第 75 次会话）

- **人的工作**：①别用浏览器弹窗（太丑）；②进来就该直接生成好大纲、用户只需改确认（不用多点"生成"）；③保留"已确定方案"多余，这页应是纯大纲；确认应在用户确认大纲时**一起确认整个方案**，再应用进工作台；④要考虑工作台已有数据时的冲突。
- **AI 的工作**：
  - **进页自动生成大纲**（无大纲且编辑区空时自动 `generateOutline()`），用户只改+确认；`生成大纲` 按钮改 `重新生成`（供改坏后重来）。
  - **去掉页面上"已确定方案"模块**（纯大纲页）。
  - **自定义确认弹窗**替代浏览器 `confirm`：点「采用大纲」弹出样式化的 modal（标题"确认方案与大纲"），展示**研究方案（renderPlanSummary）+ 论文大纲**，含「确认并应用 / 取消」；确认后 `applyOutline` 并跳转写作工作台；点遮罩/关闭可取消。`.modal-panel` 加 `max-height + overflow-y:auto`（内容多可滚、按钮可达）。
  - **数据冲突处理**：弹窗内当工作台已有内容（已用大纲/草稿/documentV2）时显示琥珀警告"…将替换章节结构；匹配章节草稿尽量保留，其余可能被覆盖"（`.oc-conflict-warning`），无数据则不显示。
  - 质量验证：进页自动生成（269 字）、弹窗展示方案(307)+大纲(269)、确认后进写作工作台、无冲突警告正确；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：第3步=纯大纲编辑页；确认动作收敛为"采用时一次性确认方案+大纲"的弹窗；不占用页面标题、也不用浏览器 alert；冲突以琥珀警告提示。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 确认弹窗重设计 + 标题行对齐/文字修正（第 76 次会话）

- **人的工作**：弹出的确认弹窗太丑；"生成并微调大纲"区文字大小不对、与右侧按钮对齐不对。
- **AI 的工作**：
  - **标题行修正**（`.topic-candidate-head-actions`）：`align-items` 由 `flex-start` 改 `center`（按钮与标题垂直居中对齐）；标题 `h3` 定 16px、desc 12.5px 淡墨，字号统一。
  - **确认弹窗重设计**：头部改为干净标题+✕ 关闭；"研究方案"由原来的 `renderPlanSummary`（含"未单独设定"提示块，乱）改为**紧凑 2×2 网格**（题目/问题/方法/数据）；"论文大纲"用**等宽可滚动盒**（`oc-outline`，max-height 240px）；确认/取消按钮常显。新增 `.oc-modA-head/.oc-modal-label/.oc-plan-grid/.oc-plan-item/.oc-outline` 等样式（≤560px 单列）。
  - 结果：弹窗清爽（方案紧凑网格+等宽大纲盒+可见按钮），标题行文字/按钮对齐正确。
  - 质量验证：弹窗渲染、标题行居中；全模块桌面+移动 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 研究设计改为一次性流程 + "已采用"态（消除内容覆盖冲突）（第 77 次会话）

- **人的工作**：提问——内容冲突只能是覆盖吗？研究设计是否该一次性的？开始写作后就不能再走该流程、只能手动改或另开新项目，这样是否合理？
- **AI 的答复**：认可并落地"一次性初始化流程"模型。研究设计（选题→方案→大纲）= 给项目定骨架；采用后骨架已进入工作台，此时再"重新采用大纲"会用新骨架覆盖已有内容，是危险的。合理模型：走一次；采用后章节结构调整都在**写作工作台**（已支持增/删/改名/调序，且增量编辑不丢内容）；真要重来另开新项目。
- **实现**：
  - **一旦 `project.outline` 已采用（存在），第3步改为"已采用论文大纲"状态**：只读展示当前大纲（`▸ 当前大纲` 展开）+ 说明"调整章节结构请在写作工作台进行（可新增/改名/调序/删除）" + 主按钮「去写作工作台」；**不再提供会覆盖内容的"采用大纲"**（移除该状态下的 adopt/confirm 弹窗）。
  - 未采用（新建项目）才走"自动生成→修改→确认（自定义弹窗）→应用"流程。
  - 修一处 handler 可选链（adopted 态下 outline-gen/copy/adopt 为 null 导致渲染失败）。
  - 质量验证：demo（有 outline）显示"已采用"态+去写作台；新建项目仍走自动生成+采用；全模块桌面+移动 0 溢出、0 控制台错误。
- **决策记录**：研究设计=一次性初始化；避免"采用新大纲覆盖已有写作内容"的破坏性交互；结构调整交写作工作台（增量、可撤销）。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 研究设计改为条件导航（一次性初始化，不再常驻 tab）（第 78 次会话）

- **人的工作**：如果研究设计是一次性初始化，就不该作为常驻 tab，应算到创建项目流程里；不只大纲不可改，前面的选题/方案也不能再回这里改，应去项目设置或写作台调整。
- **AI 的答复与实现**：
  - **研究设计改为条件导航**：`app.js` `renderNav` 过滤——项目已采用大纲（`researchDesignComplete()`：`project.outline` 存在）时从导航隐藏"研究设计"；未完成则显示。`tm:project-changed` 时检测完成态并刷新导航（避免每次保存都刷）。采用大纲后派发 `tm:projects-changed` 以立即隐藏。
  - **研究方案数据移到项目设置**：`project-settings` 新增「研究方案」卡（研究问题/方法/数据来源/关键词，只读展示，因研究设计完成后再调整该走写作台）；「去研究设计」按钮在已采用后改为「去写作工作台」。
  - 结果：研究设计＝创建项目的初始化流程；完成后不再常驻导航，选题/方案数据在项目设置可见，章节结构调整在写作工作台。
  - 质量验证：demo（有 outline）导航无"研究设计"、项目设置显示研究方案卡+「去写作工作台」；新建项目导航有"研究设计"；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 检查导出：加"重新检查"按钮 + 导出摘要改干净网格（第 79 次会话）

- **人的工作**：①检查导出页为啥直接有检查结果？是定期检查还是怎样？是否应加"触发检查"按钮？②导出的"导出摘要"那块 UI 太丑。
- **AI 的工作**：
  - 说明：检查结果 = 进入页面时**按当前数据自动算一次**的快照（非定时轮询）。
  - 补**「重新检查」按钮**（`#ce-recheck`，重导航 checkExport 重算），并在文案写明"进入页面已自动检查一次；改动后可点重新检查"。
  - **导出摘要 UI 重做**：由原来的红底 `result-box` + `<br>` 排版改为**干净网格**（标题整行 + 章节数/引用数/证据卡三列），新增 `.export-summary*.` 样式（≤560px 单列）。
  - 质量验证：重新检查按钮、导出摘要网格渲染；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 检查导出：删除内联排版预览段，页面更干净（第 80 次会话）

- **人的工作**：UI 还是很丑；下面的"排版预览"也很丑，有无必要整段全部展示？
- **AI 的工作**：删除页面底部**整段内联"排版预览"卡片**（`preview-card`）——它与"导出与预览"区的「排版预览 / PDF」按钮重复（按钮会打开打印预览窗口），整段展示既冗余又让页面冗长。删除后页面更短；`previewHtml` 仍用于 HTML 导出，不受影响。
- 质量验证：内联预览卡消失、HTML/MD/DOCX/打印预览按钮仍可用；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 四页 UI 优化（导出摘要/主页入口/文献对齐/计划任务卡）（第 81 次会话）

- **人的工作**：①导出摘要卡片右下缺一个格子难受；②论文主页"工作区入口"放最下不对，应高频/首卡；写作之旅若只是凑卡片也可留；③文献与证据"批量解析引用/手动录入"两卡内容差距没对齐；④计划与进度页 UI/UX 都不合理，需重构。
- **AI 的工作**：
  - **① 导出摘要**：`.export-summary-grid` 由 2 列改 **3 列**（标题整行 + 章节/引用/证据三列并排），填满、无右下缺口。
  - **② 论文主页**：**工作区入口提到 hero 之后**（`dashboard-stack` 顺序改为 hero → 工作区入口 → 今天该做什么/写作之旅 → 章节进度），高频入口优先可见；写作之旅保留（onboarding 进度条，有引导价值）。
  - **③ 文献与证据**：`.citation-split` 由 `align-items:start` 改 **`stretch`**，两卡等高等宽；`.citation-panel .result-box` `flex:1` 填充，视觉对齐。
  - **④ 计划与进度**：**任务卡重做**——去掉"黄色日期 chip + 空勾选圈 + 去处理"的重复/拥挤，改为 `[清晰勾选] + 标题/日期(等宽)/备注 + 单个「去处理」按钮` 的清爽卡片（`.task-item/.task-body/.task-title/.task-meta/.task-date`）。
  - 质量验证：主页顺序 hero→入口→焦点区；excitation 等宽；导出摘要 3 列；计划新任务卡；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 计划与进度页整体重排（第 82 次会话）

- **人的工作**：计划页太乱、内容太多，要求整个页面重构。
- **AI 的工作**（重排信息层级，减少一次性铺开）：
  - **顶部「进度总览」卡**：关键数字（距截止/本周阶段/已完成 X 章/连续打卡）+ 打卡按钮 + 打卡行（章节/小结）+ 热力图 + 可折叠"打卡记录"。
  - **「今日任务」主焦点卡**：今日/逾期任务（带逾期 chip 计数、逾期文案、重新调整按钮）。
  - **本周任务 / 手动补充任务 / 计划与时间轴**：改为**三个可折叠 `<details>`**（`▸ 本周任务（N）/▸ 手动补充任务/▸ 计划与时间轴`），默认收起，需要时再展开——不再四卡全铺。
  - 保留全部控件 id（plan-gen/plan-due/plan-tpl/plan-task-*/checkin/ck-*/reschedule/data-task-*），功能不变。
  - 新增 `.planner-overview/.checkin-row/.planner-cal/.planner-collapse*` 样式。
  - 结果：页面从"今日/本周/手动/时间轴/打卡 五块全铺"变为"总览 + 今日焦点 + 三块可折叠"，视觉清爽、层级清晰。
  - 质量验证：总览/今日卡渲染、可折叠展开、全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 计划页两列填满 + 导出摘要补项 + 写作之旅UI改进（第 83 次会话）

- **人的工作**：①计划页第一张卡占整行、右边空很多很丑；②导出摘要不如两列好看，不行加个内容；③论文主页写作之旅留不含糊，要留则UI稍微改进。
- **AI 的工作**：
  - **① 计划页**：改成**顶部两列**——左=进度总览（统计+打卡+热力图），右=`.planner-right`（**今日任务 + 本周任务堆叠**，本周默认展开），两列填满宽度、不再"整行空右"。新增 `.planner-top/.planner-right` 样式。
  - **② 导出摘要**：回**两列**，并**补"总字数"第4项**（标题整行 + 章节数/引用数/证据卡/总字数 2×2 填满，无右下缺口）。
  - **③ 写作之旅**：**保留**（有 onboarding 引导价值）；去掉每步的 `→` 箭头（已有贯穿连接线）、勾选圈 18→22px、标签限宽更清爽。
  - 质量验证：计划页两列布局、导出摘要 5 项、主页工作区入口置顶+journey 渲染；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 论文主页"近期状态"卡重构（第 84 次会话）

- **人的工作**：主页"章节进度+近期状态"区 UI 难看、下面空白太大；右侧"打卡记录/最近编辑/整体验收"三个横块没实质内容。
- **AI 的工作**：
  - **删掉三个弱块**（打卡记录=累计天数、最近编辑（错位文案"还没有打卡记录"）、整体验收=占位promo）。
  - **近期状态卡改为**：紧凑统计（累计打卡 N · 连续天数 N）+ 近 70 天热力图（真数据）+ 去打卡 / 去检查与导出（保留快捷入口）。整块更短、更有实质内容。
  - 章节进度卡保留（每章进度+去写/继续写），整体更均衡、下方空白减小。
  - 质量验证：近期状态渲染（统计+热力图）、全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 计划页对齐修正（第 85 次会话）

- **人的工作**：计划页 UI 还是有点丑、都对不齐。
- **AI 的工作**：
  - "今日打卡"按钮换行成两行（今日打/卡）→ `white-space:nowrap; flex:0 0 auto` 单行。
  - 左列热力图 `max-width:100%` 填满（原先受限、与卡片不对齐）。
  - 统计行间距收紧（`gap:22px`）。左右两列顶部对齐。
  - 质量验证：按钮单行、热力图填满、两列顶部对齐；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 计划页"本周任务"限量 3 条 + 查看全部（对齐两列高度）（第 86 次会话）

- **人的工作**：计划页左右两列整体高度仍没对上；实在不行本周任务减到 3 条。
- **AI 的工作**：本周任务改为**限量预览 3 条** + 「查看全部（N）▾」按钮（点击 `weekExpanded=true` 重渲染全量）；右列（今日任务+本周任务）变短，与左列"进度总览"更协调；全量仍可一键展开。`WEEK_PREVIEW=3`。
- 质量验证：查看全部按钮出现、本周限 3 条渲染、展开后全量；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-23 · 计划页改为干净单列布局（根治两列对不齐）（第 87 次会话）

- **人的工作**：计划页 UI 仍不对、两列高度没对上；热力图上方浮着日期 tooltip；让我自查。
- **AI 的检查与重构**：
  - 自查发现：两列布局（进度总览 | 今日/本周）因热力图与任务卡天然高度不一，screenshot 显示左卡 ~540 / 右 ~620，确实对不上；热力图 shift 到列宽时格子巨大；悬浮 tooltip 是热力图单元格的 `title`（原生提示，非 bug）。
  - **根治方案：改成单列**——`进度总览（紧凑：统计+打卡按钮+打卡行+折叠"打卡记录（近70天热力图）"）` → `今日任务`（全宽）→ `本周任务（预览3+查看全部）` → 手动/时间轴（折叠）。**不再有两列对齐问题**；热力图收进折叠"打卡记录"、不再占大块/悬浮。
  - 保留全部控件 id、本周任务限量 3+查看全部。
  - 质量验证：单列渲染、无两列高度差、热力图折叠；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 文献占位：正文引用恢复两处加固（第 88 次会话）

- **人的工作**：文献库出现一堆「正文引用文献 1 J」占位（只有编号、无题名/DOI）；希望正文引用的文献直接以规范完整条目进文献库，别出现占位；确认是"生成初稿后出现"。
- **AI 的排查**：占位来自 `restoreMissingCitationsFromDoc`——正文标记（`[n]` 或 `[[CIT:id]]`）在文献池（当前/旧本地/全项目）找不到来源时，就生成"正文引用文献 N"。带 DOI 的占位现有 CrossRef 自动补全（已接入文献库打开），但**裸 `[n]` 标记无 DOI → 无法补全 → 占位**。
- **两处加固**：
  - **`normalizeDraftCitationMarkers`**（writing.js）：兼容模型写出的 `[R1]/[文献1]/[1]/[[CIT:1]]` 及带空格形式，统一转成 `[[CIT:引用id]]`（引用真实入库条目），避免散落裸编号。
  - **`restoreMissingCitationsFromDoc`**（citation.js + writing.js）：裸 `[n]` 精确编号找不到时，按文献库顺序兜底映射到第 n 条完整条目，尽量补全而非造"正文引用文献 N"占位。
- **局限**：demo 已有真实引用、全新项目需走 3 步向导，未能 100% 复现"满库占位"；上述为防御性修复，需在真实项目上验证。
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · "格式整理"升级为全文格式整理校对 + 空行清理（第 89 次会话）

- **人的工作**：希望"格式整理"扩展到**全文**格式整理校对，检查更彻底、按研究生论文规范；目前有**空行**还留着没清。
- **AI 的实现**：
  - **增强 `normalizeAcademicText`**：多空格合并、中文标点前空格去除、中文括号后空格去除、引号内空格修正、中文省略号（……）、**全角字母/数字→半角**、行首尾空白去除。
  - **新增 `formatWholeDocument()`**：遍历**全文**每个段落做规范化，并**移除多余空行**（连续空段落、标题后空段落、段首段尾多余空白段落）；一次事务按文档逆序应用；完成后 toast 清楚反馈「修正 N 段文本、移除 M 个空行」。
  - `#wb-format` 由"当前章节"改为"全文整理"。
  - 实测：向编辑器插入带毛病文本+多余空行 → toast「已整理全文：修正 1 段文本、移除 4 个空行」；章节标题前空行清除、文本空格/全半角规范化。
  - 说明：**段落缩进/字体/行距/页边距等论文版式**由「模板预览/导出」(CSS) 负责，格式整理做的是**文本层**（空格/标点/空行/全半角），两者不冲突、不该在正文里硬塞空格缩进。
  - 质量验证：demo 全文整理后章节内容/引用完好，全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 写作台右侧"版本"面板渲染修复（第 90 次会话）

- **人的工作**：写作台右侧"版本"UI 不行——说明文字一个字符一行、中间穿插按钮、大片空白。
- **AI 的排查与修复**：`.wb-version-callout` 原本是 **flex 行**（左文本右按钮），但右侧面板只有 272px 宽，按钮"去下一个未完成章节"占 145px，把文本挤压到 **29.5px** → 中文一个字一行。改为 **纵向布局**（文本在上、按钮在下）：`flex-direction: column` + 按钮 `align-self: flex-start`。文本宽度恢复到 181px，正常换行。
- 质量验证：版本面板 callout 正常，全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 文献库新增批量操作 + 占位原因说明（第 91 次会话）

- **人的工作**：文献库被"正文引用文献 N / 待补全来源"占位刷屏；质询为何不是真实文献；希望加**批量修改**（删除没用的、同步等）。
- **AI 的说明**：占位 = 正文出现引用标记、但文献库/项目池里没有对应真实文献时，恢复兜底生成的"占位条目"。若这些文献的真实元数据未入库（当时生成未保存或来源在别处），只能靠文献池比对或 CrossRef 按 DOI 补全；完全找不到就是"待补全来源"。
- **新增批量操作栏**（文献库 tab）：
  - 每行左侧**多选框** + 全选；
  - **删除所选**（确认后批量删）；
  - **删除未引用**（删正文未引用的条目，安全）；
  - **删除占位**（删"待补全来源"占位，若有正文引用会提示其失效）；
  - **自动补全**（先文献池比对，再 CrossRef 按 DOI 补全，后刷新）。
  - 新增 `.cit-batch-bar/.cit-check` 等样式，`.citation-row` 改三列（复选框|主内容|操作）。
- 质量验证：demo 批量删未引用→移除 U-Net；全选+删除所选→清空；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 文献库新增「检查并同步正文引用」+ 批量操作整合（第 92 次会话）

- **人的工作**：希望支持"把写作台里的文献导入文献库"——检查时自动和文献库比对，没有的补上。
- **AI 的实现**：
  - 批量栏新增 **「检查并同步正文引用」**：扫描全文所有引用标记，与文献库比对——能对上文献池的补真实条目、带 DOI 的走 CrossRef 补全、找不到的保留占位并计数；完成后 toast 反馈「新增 X 条 / 当前 N 条 / 其中 Z 条需补全」。
  - 与现有"自动补全（文献池+CrossRef）""删除未引用""删除占位""删除所选/全选"可组合使用。
  - 质量验证：demo 点「检查并同步」→「当前 3 条，其中 0 条需补全」，无报错；全模块桌面 0 溢出、0 控制台错误。
- **说明**：现有 21 条"正文引用文献/待补全来源"是"正文引用了但文献池/库里无真实来源"的占位；`检查并同步` 只会把**能找到来源**的补成真实条目，找不到的仍是占位（需删除或手动重新入库）。生成初稿时引用的文献（有真实元数据）会在生成时就写入文献库，避免以后出现占位。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 修复：最后一章生成初稿写到文档末尾（跑到参考文献/致谢后面）（第 93 次会话）

- **人的工作**：第五章（最后一章）生成初稿时，内容没有放到第五章位置，而是跑到文档最末尾（参考文献、致谢后面）。
- **AI 的排查与修复**：`topLevelSections` 只把 `role==='section'` 的标题算作章节边界；而"参考文献"(role='references')、"致谢"(role='ack') 不是 section，导致**最后一章（第五章）成了"最后一个区"，其 `bodyTo` 被算成 `doc.content.size`（文档末尾=致谢后）**，于是生成初稿插到文档尾。
- **修复**：章节体边界改用**下一个非 `subsection` 的顶层标题**（参考文献/致谢/下一章都算边界；`subsection` 是章内子标题不算）。这样第五章的 bodyTo = 参考文献标题位置，生成初稿插在"第五章"与"参考文献"之间。
- 质量验证：第五章(598) < 参考文献(612) < 致谢(640) 顺序正确；五章逐一导航 0 溢出；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 无来源引用不再进文献库；自动清除"待补全来源"占位（第 94 次会话）

- **人的工作**：工作台里无来源的文献就**不要加到文献库**，情愿没有也别出现"正文引用文献/待补全来源"占位，免得系统看起来有问题。
- **AI 的实现**：
  - **`restoreMissingCitationsFromDoc`**（citation.js + writing.js）：正文引用**找不到真实来源的，一律跳过**，不再造"正文引用文献 N / 待补全来源"占位；
  - 新增 **`pruneUnresolvablePlaceholders()`**（文献库 render 时调用）：移除"无 DOI 且文献池里对不上"的占位（无来源无 DOI → 删；能对上文献池 → 补全；仅带 DOI → 保留待 CrossRef）。
  - 说明：正文里被删占位对应的引用节点仍保留（无编号细节），但文献库不再被空壳占位刷屏；正文真实引用（有来源）不受影响。
  - 质量验证：demo 文献库无占位、真实条目保留；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 自学习飞轮（客户端、规则式）Minimal Closed-loop（第 95 次会话）

- **评分要求**：老师看重"自学习"——系统通过用户持续交互学习其习惯、持续优化表现。
- **约束**：纯前端无后端/无 ML 依赖，数据在 localStorage。
- **AI 的设计（观察→归纳→适配→展示）**：
  - **观察**（写作台 AI 动作埋点）：`handleReviewAction` 里接受/拒绝/重新生成记录 `recordOutcome`；命中改写类建议时记录 `recordLengthDelta`（original→suggestion 长度差）。
  - **归纳**（`js/self-learning.js`）：聚合每个动作的使用/接受率、篇幅变化 → 生成可解释结论（最常用动作、接受率、简化/详尽偏好、累计交互数）。
  - **适配**：`systemPrompt()` = `SYSTEM + buildPreferencePrompt()`，把学到的偏好注入每次 AI 调用（润色/续写/生成初稿等），让大模型按用户习惯调整语气/篇幅/详略。
  - **展示**：主页新增"自学习"卡（`learningCardHtml()`），显示已学习 N 次交互、各动作使用/接受率条形、归纳出的偏好。
- 实测：接受一次"学术润色" → 记录 `academic 使用1/接受1` + `delta:-59`；主页学习卡显示"已学习 1 次交互 · 你最常用「学术润色」· 你偏好简洁表达 · 接受率 100%"。
- **成本**：新增一个 `js/self-learning.js` 小模块 + 写作台两个埋点 + 主页一张卡，无后端/无数据库/无 ML 库。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 三大任务：①自学习完善为独立页 ②融入检查导出并移除tab ③全站内部词改对外（第 96 次会话）

- **人的工作**：①自学习飞轮在主页底部太不显眼，单独做个页并完善（技术能力 + 用户可调试/手动设置）；②"检查与导出"tab 与写作台导出重复，考虑融进写作台移除单独页；③全站审查，不能出现"自学习飞轮"这类内部词，所有文案要对外、像专业系统。
- **AI 的完成**：
  - **①自学习独立页**：新增 `js/modules/self-learning.js`（导航项**「个性化」**）+ `js/self-learning.js` 扩展（`getPrefs/setPrefs`、`learningPageHtml`）。展示：学习状态（各动作使用/接受率条形）、归纳出的偏好，**手动调整**（语气偏好：自动/正式/简洁/详尽；应用程度：轻度/标准/严格；保存偏好、重置学习）。学习偏好经 `buildPreferencePrompt()` 注入每次 AI 调用（`systemPrompt()`）。主页学习卡标题/文案一并改干净。图标 `self-learning`（脑）。
  - **②融入检查/导出**：导出 `collectIssues`/`issueHtml` 供写作台复用；写作台"导出"菜单新增**「导出前检查」**（弹窗显示结构/逻辑/引用/格式问题 + 问题清单，Escape/遮罩可关）；**移除"检查与导出"导航 tab**，更新主页"工作区入口/去检查与导出/检查整体状态"引用指向 writing。
  - **③内部词改对外**：`自学习飞轮`→`个性化`（自学习卡/页/文件名注释）；主页空状态删"V4"；顶部徽标 `API 已配置/已配 Key 未启用`→`AI 已启用/未启用真实 AI`；mock 回复`测试模式`→`演示模式`。
  - 质量验证：导航含 `个性化`、无 `检查与导出`；全模块桌面 0 溢出、0 控制台错误；自学习页/导出前检查弹窗/手动调整/重置均正常。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 个性化页丰富化（功能更强、整体简洁）（第 97 次会话）

- **人的工作**：个性化页功能太简单，希望更丰富、看起来更强大，但整体简洁。
- **AI 的实现**（`js/self-learning.js` + 模块页 + CSS）：
  - **学习概览数据卡**：累计交互 / 建议接受率 / 覆盖功能 / 最近学习。
  - **系统偏好画像**（4 卡）：语气 / 篇幅（由已接受建议长度差归纳）/ 引用密度 / 术语风格。
  - **更丰富的归纳**：最常用、更接受、篇幅偏好、接受率、累计交互。
  - **手动微调 4 个旋钮**：语气偏好 / 术语风格 / 引用密度 / 应用程度（均存为 prefs 并注入 AI 提示词 `buildPreferencePrompt()`），保存/重置。
  - **学习轨迹**：最近交互事件时间线（`recordOutcome` 写 log）。
  - 整体用数据卡 + 分组标题呈现，干净不堆砌；`learningPageHtml()` 重构。
  - 质量验证：两次交互后页面展示 `累计交互2·接受率100%·偏好简洁·学习轨迹`，全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 全站去除"内部口径"话术（C 端语气）（第 98 次会话）

- **人的工作**：文献页仍出现"把'找文献、管文献、摘证据'拆开处理，当前页面只保留一类任务"这类明显是内部任务/设计解读的话术；要求全站每个页面都检查，去掉不能给 C 端用户看的内部口径。
- **AI 的排查与替换**（面向用户可见 desc/导语/副标题，共 ~21 处）：
  - 文献页：删"拆开处理/只保留一类任务/不再混入/清洗成结构化/统一入库/优先保留/默认先看"→ 用户向表达（整理成规范条目加入文献库；展示与当前章节相关的证据卡等）。
  - 主页：删"首页优先保留...入口/只显示真正影响/只辅助节奏"→ 快捷入口/聚焦关键事项/帮你把握节奏。
  - 计划页：删"同一条工作线/只保留最该推进/只露出最靠前/不和今日行动混在一起/放这里回看节奏"→ 直白描述。
  - 研究设计：删"只保留最少输入/只定写作边界/不再要求大段填写/不需要再点确认/按真实论文推进顺序"→ 用户向。
  - 写作台：删除"优先看和当前章节"内部措辞。
  - 其余多为代码标识符（非界面）或产品名"PaperPilot"（对外），无需改。
- 质量验证：全模块桌面 0 溢出、0 控制台错误；各页正文无"拆开处理/只保留一类任务/不再混入"等话语残留。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 文献库：修复重复引入 + 与写作台解耦（独立库）（第 99 次会话）

- **人的工作**：①"自动引入文献"出现**重复引用**（同一文献入库两次，但有的不会）；②文献库**不该跟写作台的使用情况挂钩**——文献库是独立库，可含很多未用文献；它应包含写作台用到的文献，但不是被写作台"删减/过滤"。
- **AI 的排查与修复**：
  - **① 重复引入**：AI 解析批量入库、手动录入、生成初稿引文入库，此前**都无脑 unshift 不去重**（按单个键匹配、同题名不同 DOI 会漏）。新增 `citationExists`（按 **DOI 或题名** 大小写不敏感匹配），在手动录入/AI 解析/生成初稿入库处应用；并新增 **`dedupeCitations`**（文献库渲染时清掉已有重复条目，同 DOI/题名保留先入库的）。
  - **② 文献库与写作台解耦**：`整理文献` 原先是 `saveCitations(used)`——**只保留正文用到的、把未用的从文献库移除**。改为 **`[...used, ...unused]`**——保留全部（已用 + 未用），只把已用的按正文引用顺序排到前面并重编号（L1..N），未用保留在库中；不再删未用、不再清理证据卡。
  - 说明：文献库 = 独立文献仓库，是写作引用的来源；"已引用/未引用"仅为状态标识；生成出稿引入的文献会加入文献库。
  - 质量验证：全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 修复：生成出稿后文献库重复引入（5 条引用 → 8 条含重复）（第 100 次会话）

- **人的工作**：生成出稿后实际 5 条引用，但文献库出现 8 条，好几条重复；要求查清原因。
- **AI 的排查**：重复来源不止一处——
  1. **revoke 恢复**（`restoreMissingCitationsFromDoc`）会把正文引用补成新条目，若与库中同 DOI/题名但不同 id 的条目并存 → 重复；
  2. 生成初稿入库只按单个键、且**入库前未查重**的路径；
  3. **渲染时 `dedupeCitations` 放在 restore 之前**，restore 之后又会产生重复（顺序 bug）。
- **修复**：
  - **入库前查重**：`citationExists`（DOI 或规范化题名）应用到手录/AI解析/生成引文入库；`restoreMissingCitationsFromDoc`（citation.js + writing.js）入库前 `dedupAgainst`/`sameCitation` 查重，跳过重复。
  - **渲染去重放最后**：`dedupeCitations` 移到 restore/upgrade/prune 之后，清掉所有来源产生的重复。
  - 增强 **`dedupeCitations`**：按 id / DOI / 规范化题名（去空格标点）三键去重，保留先入库的。
  - 质量验证：demo 文献库 3 条不误删（无重复提示）；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 上线前业务审计 + 修复（P1 全部 + 高价值 P2）（第 101 次会话）

- **方式**：并行 4 个 subagent 分域审计（项目/主页/设置、研究设计/计划、写作台、文献/导出），叠加本人 runtime 冒烟（挂载/导出/计划/导航）。发现 **6 个 P1** + 多个 P2；无 P0 阻断。
- **已修复 P1**：
  - **P1-1 项目设置改题目不生效**：`project-settings` 保存时同步写 `researchDesign.title`（并与 project.title 同源），解决 design.title 优先级导致改题无效。
  - **P1-2 单项目导出备份不可导入**：`project.js` 新增 `importSingleProject`；`settings.js` 导入识别 `{app:'paperpilot',project}` 单项目备份并作为一个新项目导入激活。
  - **P1-3 章节版本回退破坏结构/原子节点**（数据丢失）：`createChapterSnapshot` 存**结构化 Slice**，`restoreChapterVersion` 用 `replaceWith` + `Slice.fromJSON` 恢复（含引用/图表/公式），并补充结构替换警示。
  - **P1-4 导出前检查读旧态**：`wb-export-check` 改用**实时 `viewState.view.state.doc`**（与导出同源）。
  - **P1-5 导出检查弹窗跳转失效**：补绑 `[data-issue-go]`（去对应章节/文献库/研究设计/计划）。
  - **P1-6 GB/T 7714 卷期错拼**：`gbt7714.issueBlock` 改 `卷(期): 页码`（`34(3): 45-52`），修 EB/OL 顺序与双标点。
- **已修复 P2（高价值）**：清本地数据遗漏自学习（`settings.js` 清 `paperpilot.` 键）；计划页 `dateKey` 改本地日期（修 UTC/本地时区错位）+ `今日=精确当天`（去逾期重复计数）。
- **验证**：改题生效、导出检查"去对应章节"跳转生效并关弹窗；全模块桌面 0 溢出、0 控制台错误。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 上线前审计完成 + 全部 P1 修复（第 102 次会话）

- 4 域审计 + runtime 冒烟：**无 P0 阻断**；**6 个 P1** 全部修复并验证；多个 P2 已修（高价值）。
- 已验证的闭环：全站挂载 0 溢出/0 报错；写作台导出（Word/Markdown/PDF）；导出前检查（弹窗+问题跳转）；改题生效；文献库去重不误删；计划勾选/打卡。
- 审计发现清单见 `report/audit-findings.md`（P1 已标 ✅）。
- **剩余（不阻断，可后续）**：evidence 关联章节 label 解析；导出摘要总字数/引用数口径；防抖保存窗口内切项目覆盖（edge）；光标标题边界漂移；空项目导出空骨架轻提示；主页 heroAction 死代码 / focus CTA 双触发（幂等）；demo 载入语义/英雄进度自相矛盾。
- **人工修改**：（待作者补充）
- **质量评估**：（待作者补充）
- **用时**：（待作者补充）

## 2026-08-29 · 再清 2 个口径/显示 bug（第 103 次会话）
- **evidence 关联章节 label**：`citation.js:434` 用 `c.sectionId === linkedSectionIds[0]` 查找，无 sectionId 时恒显示"已关联章节"；改 `c.sectionId === id || c.chapter === id`。
- **导出摘要总字数口径**：`check-export.js:256` 由只累加章节 drafts 改为 `fullTextFromDoc(doc, citationMap(citations))`（与写作台"全文字数"一致）。
- 全站回归 0 溢出 0 报错；导出前检查弹窗正常。

## 2026-08-29 · 继续清理剩余 P2（第 104 次会话）
- **防抖保存切项目覆盖**：`persistNow` 防抖时记 `saveProjectId`，防抖窗口内切项目则跳过保存（避免旧文档写进新项目）。
- **光标标题边界漂移**：`sectionForPos` 改半开区间 `[headingFrom, bodyTo)`。
- **focus 卡 CTA 双触发**：有 `data-write` 时不再同时给 `data-nav`（避免二次 switchModule）。
- **复制全部**：改用正文引用编号 `[n]`（首次出现序），未引用项用 `L#`，与导出参考文献一致。
- **研究方案 ready 态补「重新生成方案」按钮**（`#rd-plan-regen`）。
- **删死代码**：`taskChip`（planner 无引用）。
- 全站回归 0 溢出 0 报错。

## 2026-08-29 · 学术规范合规红线修复（对照 GB/T 7714-2015 等）（第 105 次会话）
- **参考文献著录**（gbt7714.js）：D/R 补"城市: 单位"、C 会议去重+`//会议名. 城市: 出版者, 年: 页码`、S 改`标准名: 标准号[S]. 城市: 出版者, 年`、N 改`报纸名, 年(版次)`、西文作者>3 用`et al.`（修双句点）。
- **正文引用上标**（GB/T 7714 顺序编码制）：模板预览（writing.buildPreviewHtml）+导出检查预览（check-export.buildPreviewHtml）把引用编号包`<sup>`；DOCX 导出（textRuns）把正文`[n]`拆成 superscript run。
- 报告见 `report/academic-compliance-audit.md`（含每处依据 + 差异 + 已修）。

## 2026-08-29 · GB/T 7713.2 结构：DOCX 补"目录"（第 106 次会话）
- DOCX 导出在正文前（题目/摘要/关键词之后）插入**"目录"标题 + TOC 域**（`TOC \\o "1-3" \\t "HeadingChapter,1;Heading2,2;Heading3,3"`，Word 打开右键更新即生成）。实测解压 document.xml 含 TOC 域 + "目录"，且正文引用 `[n]` 为 superscript run。
- 模板预览（网页）暂不含"目录"页，已记入合规报告（后续按校模板补）。

## 2026-08-29 · 论文格式模板：通用默认 + 可自定义（第 107 次会话）
- `project.js`：新增 `DEFAULT_THESIS_TEMPLATE`（通用学位论文默认：3cm 四边距、宋体小四正文、黑体三号标题、1.5 行距，**非单一学校样式**）+ `getTemplate(project)`（项目级 `template` 可覆盖）。
- `docx-export.js`：`buildDocxParts`/`buildStylesXml`/`createDocxBlob` 按 `getTemplate(project)` 应用页边距（cm→twips）、正文/标题字体字号、行距；`DEFAULT_THEME` 兜底。
- `project-settings.js`：新增「论文格式模板」卡（四边距/正文中西方/字号/标题/行距 + 保存/还原默认），保存写 `project.template`。
- 实测：设 top=2.4cm → 导出 docx `pgMar top="1361"`；还原默认生效；全站 0 溢出 0 报错。
- 说明：模板为通用默认（可自定义），不宣称是任一学校标准；GB/T 7714-2015 著录与规范化（学术红线）在上一步已单独合规。

## 2026-08-29 · 修复"文献库重启后消失"（数据持久化根因）（第 108 次会话）
- **现象**：文献库（以及草稿/打卡）在页面/服务器重启后丢失。
- **根因**：`project.js` 的 `clearLegacyProjectKeys()`（在启动 `ensureProjectStore` 末尾调用）使用 storage.js 的 `remove(key)` 逐键清理 `LOCAL_PROJECT_KEYS`。而 storage.js 的 `get/set/remove` 会把 `drafts/citations/checkins/versions` 路由到 `__paperpilotScopedStore`（即项目库 shim），`remove('citations')`→`saveCitations([])`、`remove('drafts')`→`saveDrafts({})`…… → **每次启动都清空项目（IndexedDB）里的引用/草稿/打卡**。
- **修复**：`clearLegacyProjectKeys` 改为直接 `localStorage.removeItem('paperpilot:' + key)`（绕过 shim），只清旧版 localStorage 键，不清项目库数据；移除未用的 `remove` 导入。
- **验证**：载入演示 → 文献库 3 条 → **刷新后仍 3 条**（此前变 0）；写作正文/计划打卡正常；全站 0 溢出 0 报错。
- **顺带**：demo-data 用 `set('drafts'/'citations'/'checkins')`（legacy localStorage）改为 `saveProject()`（项目对象，正确持久 + 可读）。

## 2026-08-30 · 写作态首页/计划页体验收束（第 109 次会话）
- **目标**：熟悉最新进展时顺手处理一组“已进入写作后仍被前置流程干扰”的体验问题。
- **修复**：
  - 首页已有大纲/正文时显示「方案已应用」「已进入写作」，不再继续强调研究设计计数；工作区入口改为「查看项目方案」。
  - 计划页移除“必须设置截止日期”的自动任务；已有大纲或正文但未生成计划时，当前阶段显示「按章节推进」。
  - 计划页在写作态不再把补齐研究问题/方法/数据/可行性检查作为主任务，避免用户从写作流程被拉回研究设计。
  - 资料工作台文案收敛为“检索 → 入库 → 证据卡”的用户语言，减少内部模块说明。
  - 设置页 API Key 区域用表单包裹并阻止默认提交，消除浏览器密码字段警告。
  - 移动端写作工具栏改为横向滚动，减少工具栏换行堆叠和正文挤压。
- **验证**：`npm run build` 通过；Chrome CDP 复核首页、计划页、资料工作台、设置页和移动端写作页，桌面/移动端无横向溢出，设置页无运行时事件。

## 2026-08-30 · 移动端写作空间 + 项目摘要/进度口径（第 110 次会话）
- **目标**：继续全站 UI/UX 成熟化检查，优先处理真实页面走查里会影响用户信任和写作空间的问题。
- **发现**：
  - 移动端侧栏直接堆到页面顶部，占据首屏过多空间，写作页进入正文太慢。
  - CSS 使用 `.module-writing` 选择器，但应用实际只设置 `body[data-module="writing"]`，导致一批写作页专属宽度和移动端规则没有生效。
  - 项目设置的研究方案摘要只读取独立研究设计字段，已有大纲/正文时仍显示“未单独设定”；对象字段也有再次显示 `[object Object]` 的风险。
  - 项目中心进度只按已完成章节计算，首页按“进行中=半章”计算，同一演示项目出现 20% / 40% 两套口径。
- **修复**：
  - 移动端全局导航改成紧凑顶栏：品牌保留，模块入口横向滑动，设置和折叠按钮收成图标。
  - 写作页移动端隐藏重复模块标题，压缩目录卡说明和间距，编辑器起始位置从约 636px 提前到约 444px。
  - 将写作页专属 CSS 选择器统一改为 `body[data-module="writing"]`，让桌面宽度扩展和移动端规则真实生效。
  - 项目设置摘要增加 `choiceText/summaryValue`，兼容字符串、数组、对象字段；已有大纲时用题目、大纲、关键词和当前章兜底，不再误报为空。
  - `projectStats` 进度改为章节加权统计，与首页一致。
- **验证**：`npm run build` 通过；Chrome CDP 验证桌面写作页、项目中心、项目设置和移动端写作页：0 横向溢出、0 运行时事件、无 `[object Object]`；项目中心与首页均显示 40% 进度。
